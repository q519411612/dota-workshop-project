---
phase: 03-safe-candidate-assembly
plan: 06
type: tdd
wave: 6
depends_on: ["03-05"]
files_modified:
  - tests/release-candidate.test.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCFS-05
must_haves:
  truths:
    - "Users receive an explicit source-changed or unsafe-entry failure whenever either source tree changes before, during, or after assembly."
    - "No source-change case is retried into a mixed candidate, and no success or failure path writes, repairs, renames, compiles, or deletes source content."
    - "Phase 3 fixture, policy, compatibility, type, build, and repository quality gates all pass without touching user-owned graph changes."
  artifacts:
    - path: "src/release-candidate.ts"
      provides: "Per-entry observation comparison, final rewalk, no-retry failure, and source-write confinement"
      exports: ["withAssembledReleaseCandidate"]
    - path: "tests/release-candidate.test.ts"
      provides: "Mutation checkpoint, destination failure, source immutability, and no-retry tests"
  key_links:
    - from: "precreation source inventory"
      to: "immediate, post-copy, post-callback, and final-rewalk observations"
      via: "stable topology/type/stat/byte comparison used only for change detection"
      pattern: "SOURCE_CHANGED_DURING_ASSEMBLY"
---

<objective>
Fail closed on every observable source mutation while proving that candidate assembly never writes to either source tree.

Purpose: Close RCFS-05 and run the complete Phase 3 quality gate after the strict lifecycle is integrated.
Output: Deterministic mutation checkpoints, immutability evidence, no-retry behavior, and a fully passing local phase suite.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-safe-candidate-assembly/03-CONTEXT.md
@.planning/phases/03-safe-candidate-assembly/03-RESEARCH.md
@.planning/phases/03-safe-candidate-assembly/03-PATTERNS.md
@.planning/phases/03-safe-candidate-assembly/03-05-SUMMARY.md
@src/release-candidate.ts
@tests/release-candidate.test.ts
@tests/release-readiness.test.ts
@tests/preflight.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify mutation detection and source immutability</name>
  <files>tests/release-candidate.test.ts</files>
  <behavior>
    - Add, remove, rename, retype, link-replace, truncate, and same-length byte mutations at deterministic pre-revalidation, pre-copy, post-copy, callback, and final-rewalk checkpoints fail explicitly.
    - Mutation failures do not retry, do not invoke inspection before a stable assembly, and still end the callback-scoped candidate lifetime.
    - Source topology and bytes are identical before/after success, destination copy failure, callback failure, and removal failure; recorded writes never fall under source roots.
  </behavior>
  <action>Add narrow lifecycle checkpoint injection to tests for the required mutation timing without creating a general virtual filesystem. Snapshot source topology and bytes in the harness, record every mkdir/copyFile/rm destination, and assert stable code plus safe relative identity. Include a same-size content mutation so stat-only checks cannot satisfy the contract. RED command: `npm test -- tests/release-candidate.test.ts -t "fails on source mutation without writing source trees"`. Expected RED: one or more injected mutations reaches callback/success, retries, or lacks an explicit source-changed blocker because final stable observations are incomplete; unexpected GREEN requires stopping. Commit only the test changes as `test(03-06): require immutable source assembly`, excluding all graph files.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "fails on source mutation without writing source trees"</automated>
  </verify>
  <done>The mutation matrix records the expected RED failure and proves the test can distinguish stat-only observation from byte change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-closed stability checks and run all gates</name>
  <files>src/release-candidate.ts</files>
  <behavior>
    - Stable entry observations are compared immediately before use, after each copy, after inspection, and in a final sorted rewalk of both roots.
    - Topology, type, canonical identity, stable stat identity, or byte changes return SOURCE_CHANGED_DURING_ASSEMBLY or the more specific unsafe-entry blocker without retry.
    - Every production write is confined beneath the validated candidate root and boundary evidence disclaims mutation, build, Steam, Workshop, archive, signing, encryption, launch, and runtime actions.
  </behavior>
  <action>Complete lifecycle checkpoint comparisons using adapter-provided kind, canonical identity, stable stat fields, and byte observations used solely for source-change detection; do not expose a hash, digest, manifest, or integrity claim owned by Phase 4. Rewalk both roots after copy and after callback, compare the full sorted identity set, and fail on additions, removals, renames, retypes, or content changes. Do not retry, recopy, repair, compile, or write beneath source roots. Make the focused and complete candidate suites GREEN, then run every command in the plan verification block. Inspect `git status --short` and `git diff --cached --name-only`; preserve and exclude `.planning/graphs/`. Commit only the source change as `feat(03-06): detect source mutation` after all focused implementation gates pass.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts tests/release-readiness.test.ts tests/preflight.test.ts tests/install-simulation.test.ts tests/source-snapshot.test.ts tests/result.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>All mutation and immutability tests pass, the atomic source commit is isolated, and the complete Phase 3 gate passes with graph changes untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutable source filesystem to candidate copy | Source entries can change between inventory, classification, reading, copy, and inspection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-11 | Tampering | source stability observations | mitigate | Compare type, canonical identity, stable stats, bytes, and final topology without retry. |
| T-03-12 | Elevation of Privilege | destination write confinement | mitigate | Validate every production write as a descendant of the canonical candidate root. |
</threat_model>

<verification>
Run, in order: `npm test -- tests/release-candidate.test.ts tests/release-readiness.test.ts tests/preflight.test.ts tests/install-simulation.test.ts tests/source-snapshot.test.ts tests/result.test.ts`; `npm test`; `npm run typecheck`; `npm run build`; `npm run verify:plugin`; `npm run verify:source-snapshot`; `npm run verify:install-simulation`; `npm run verify:rc`; `npm run verify:handoff`; `npm run verify:milestone`; `git diff --check`. Then inspect `git status --short`, confirm `.planning/graphs/` retains its pre-existing user modifications and is absent from every implementation commit, and perform an independent review of containment, link/reparse rejection, mutation timing, redaction, candidate lifetime, dry-run compatibility, and Phase 4/5 scope exclusions.
</verification>

<success_criteria>
- RED precedes GREEN with atomic test and source commits.
- RCFS-05 mutation and source-immutability behavior passes every injected timing and failure case.
- All Phase 3 focused and full quality gates pass on macOS fixtures without a Dota install or real Windows claim.
- No public MCP/schema/server/remote work, manifest/hash reconciliation, formal cleanup evidence, persistent candidate, archive, signing, encryption, credential handling, Steam/Workshop action, compilation, or source repair is present.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safely assemble complete game and content trees while sources remain unchanged | 02-06 | COVERED | Prevalidation, inventory, isolation, layout, and stability form the full lifecycle. |
| REQ | RCOP-02 | Input/root blockers before creation | 02 | COVERED | Zero-creation matrix. |
| REQ | RCFS-01 | Target-local canonical temporary isolation | 04 | COVERED | Pre/post creation containment and callback lifetime. |
| REQ | RCFS-02 | Exact complete two-root layout | 05 | COVERED | All regular files and empty directories, no heuristics. |
| REQ | RCFS-03 | Unsafe entries, identities, escapes, and collisions | 03 | COVERED | Fail-closed precreation inventory. |
| REQ | RCFS-04 | Shared readiness and redacted blockers | 01 | COVERED | Pure policy plus exact dry-run renderer. |
| REQ | RCFS-05 | Source-change failure and immutability | 06 | COVERED | Checkpoints, final rewalk, no retry, write confinement. |
| RESEARCH | — | Callback-scoped candidate lifecycle | 04 | COVERED | Candidate is inspectable only during callback. |
| RESEARCH | — | Sorted lstat/realpath traversal and explicit copyFile | 03, 05 | COVERED | Dirent is names-only; reclassification precedes use. |
| RESEARCH | — | Structured policy extraction with compatibility characterization | 01 | COVERED | Public dry-run results stay equal. |
| RESEARCH | — | Mutation checkpoints and final source rewalk | 06 | COVERED | Topology/type/stat/byte changes fail without retry. |
| RESEARCH | — | No dependency, public ToolResult, manifest, or remote expansion | 01-06 | COVERED | Explicit exclusions in actions and criteria. |
| CONTEXT | — | Validate name and all roots before creation; no fallback | 02 | COVERED | Typed validated state guards creation. |
| CONTEXT | — | Canonical candidate outside Dota, sources, and repository | 04 | COVERED | Parent and created child both checked. |
| CONTEXT | — | Explicit traversal; include all regular/hidden files and empty directories | 03, 05 | COVERED | Inventory plus fixed layout assembly. |
| CONTEXT | — | Normalized relative identities; reject traversal, links, reparse, special, escape, and collision | 03 | COVERED | Safe blocker identity and no dereference/repair. |
| CONTEXT | — | Revalidate immediately; fail on source mutation; never modify source | 05, 06 | COVERED | Per-use checks, final rewalk, write confinement. |
| CONTEXT | — | Reuse readiness policy and preserve dry-run operation | 01, 05 | COVERED | Shared findings plus compatibility renderer. |
| CONTEXT | — | Explicit safe boundaries and macOS fixture completion | 01-06 | COVERED | Local tests only; forbidden actions remain absent. |
| CONTEXT | deferred | Manifest/digest/reconciliation/coverage/cleanup precedence | Phase 4 | EXCLUDED | Correctly outside Phase 3. |
| CONTEXT | deferred | MCP registration and fixture/local/SSH/PowerShell parity | Phase 5 | EXCLUDED | Correctly outside Phase 3. |
| CONTEXT | deferred | Persistent/upload/archive/sign/encrypt/credentials/compile/repair/real Windows | — | EXCLUDED | Outside v1.14 or explicitly prohibited. |
</source_audit>

<output>
Create `.planning/phases/03-safe-candidate-assembly/03-06-SUMMARY.md` when done.
</output>
