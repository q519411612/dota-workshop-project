---
phase: 03-safe-candidate-assembly
plan: 03
type: tdd
wave: 3
depends_on: ["03-02"]
files_modified:
  - tests/release-candidate.test.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCFS-03
must_haves:
  truths:
    - "Users receive safe relative-path blockers for every link, reparse, special, unknown, escaped, absolute, traversing, ambiguous, or case-colliding entry."
    - "Rejected entries are never dereferenced, copied, repaired, or followed by candidate creation."
  artifacts:
    - path: "src/release-candidate.ts"
      provides: "Explicit entry classification, normalized candidate identities, and deterministic collision inventory"
      exports: ["inventoryReleaseCandidateSources"]
    - path: "tests/release-candidate.test.ts"
      provides: "Unsafe-entry and path-identity adapter contract tests"
  key_links:
    - from: "src/release-candidate.ts"
      to: "filesystem adapter classify and realpath operations"
      via: "sorted non-dereferencing inventory before destination creation"
      pattern: "inventoryReleaseCandidateSources"
---

<objective>
Inventory both source trees into safe normalized identities before any destination exists.

Purpose: Close RCFS-03 by failing closed on unsafe entry kinds, escapes, traversal, separator ambiguity, and case-fold collisions.
Output: A deterministic pre-copy inventory and exhaustive macOS adapter-contract tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-safe-candidate-assembly/03-CONTEXT.md
@.planning/phases/03-safe-candidate-assembly/03-RESEARCH.md
@.planning/phases/03-safe-candidate-assembly/03-PATTERNS.md
@.planning/phases/03-safe-candidate-assembly/03-02-SUMMARY.md
@src/release-candidate.ts
@tests/release-candidate.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify fail-closed source identities</name>
  <files>tests/release-candidate.test.ts</files>
  <behavior>
    - Real symlinks and injected junction/reparse, special, and unknown entry kinds return stable blockers with root-qualified relative identities.
    - Canonical root escapes, absolute identities, parent traversal, dot segments, backslash ambiguity, and case-only collisions across the complete fixed candidate identity set block before creation.
    - Rejected kinds are not realpathed through their targets, read, copied, repaired, or retried; blocker serialization excludes target contents and private roots.
  </behavior>
  <action>Extend the fixture suite with real symlinks where supported and injected adapter classifications for Windows-only kinds. Include global collisions within and across the `game/dota_addons/&lt;addon&gt;` and `content/dota_addons/&lt;addon&gt;` identity space, shuffled enumeration, and creation-call assertions. RED command: `npm test -- tests/release-candidate.test.ts -t "rejects unsafe source identities before creation"`. Expected RED: at least one unsafe kind or identity is accepted, dereferenced, or reaches creation because the inventory gate is not implemented; unexpected GREEN requires test correction. Commit only the test change as `test(03-03): reject unsafe source identities`, excluding graphs.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "rejects unsafe source identities before creation"</automated>
  </verify>
  <done>The focused test records the expected RED failure for the missing fail-closed inventory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic classification and collision inventory</name>
  <files>src/release-candidate.ts</files>
  <behavior>
    - Directory enumeration supplies names only; adapter lstat-style classification is authoritative.
    - Candidate identities use root provenance and forward-slash segments, ordinal ordering, and invariant case folding.
    - Only regular files and directories survive the inventory, and canonical containment is verified without dereferencing rejected kinds.
  </behavior>
  <action>Add explicit adapter entry kinds `file`, `directory`, `symbolic-link`, `reparse`, `special`, and `unknown`. Sort names with ordinal comparison, normalize identities before destination path construction, and maintain one collision map over the full fixed-prefix identity set. Reject absolute/platform-absolute paths, parent or dot traversal, separator ambiguity, canonical escapes, and every non-file/non-directory kind with safe blockers. Inventory both source trees fully before the creation seam; do not use Dirent type authority, localeCompare, prefix containment, recursive copy, retry, or repair. Make the focused test GREEN, then run typecheck and build. Commit only the source change as `feat(03-03): classify source identities` after staged-path inspection.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "rejects unsafe source identities before creation" &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>All unsafe entries and identities block before creation with no dereference, fallback, repair, or leaked target data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source directory entry to candidate identity | Untrusted names and entry kinds can escape roots or alias another destination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-05 | Tampering | identity normalization | mitigate | Reject unsafe segments and global case-fold collisions before path joining or creation. |
| T-03-06 | Information Disclosure | link/reparse handling | mitigate | Classify without dereferencing and omit link targets and raw errors from evidence. |
</threat_model>

<verification>
Focused unsafe-entry tests, typecheck, build, `git diff --check`, and staged-file inspection pass without touching `.planning/graphs/`.
</verification>

<success_criteria>
- RED precedes GREEN with isolated test and source commits.
- RCFS-03 blockers cover every required entry kind and unsafe identity category.
- The complete inventory is accepted before any candidate directory can be created.
</success_criteria>

<output>
Create `.planning/phases/03-safe-candidate-assembly/03-03-SUMMARY.md` when done.
</output>
