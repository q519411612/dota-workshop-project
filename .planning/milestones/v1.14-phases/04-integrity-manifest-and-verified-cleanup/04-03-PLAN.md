---
phase: 04-integrity-manifest-and-verified-cleanup
plan: 03
type: tdd
wave: 3
depends_on: ["04-02"]
files_modified:
  - tests/release-candidate.test.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCIN-04
must_haves:
  truths:
    - "The exact ledger consumes final post-inspection candidate and source occurrence observations rather than an earlier callback-visible snapshot."
    - "Users receive a blocker unless each accepted regular file maps to exactly one candidate observation and manifest entry."
    - "Missing, exact duplicate, unexpected, wrong-root/path, wrong-kind, or unobserved entries remain independently visible and cannot be collapsed by a map."
    - "Empty directories remain required by structural reconciliation while never becoming manifest file entries."
  artifacts:
    - path: "src/release-candidate.ts"
      provides: "Occurrence-count inclusion ledger composed with exact tree reconciliation"
    - path: "tests/release-candidate.test.ts"
      provides: "Missing, duplicate, unexpected, wrong-identity, and empty-directory ledger matrix"
  key_links:
    - from: "raw strict final candidate and source observation occurrences"
      to: "unique lookup map"
      via: "sort and exact occurrence counting before map construction"
      pattern: "observedFileCount|matchedFileCount"
    - from: "inventory files and directories"
      to: "artifact validation"
      via: "file ledger plus independent structural reconciliation"
      pattern: "reconcileCandidateTree"
---

<objective>
Require an exact one-to-one inclusion ledger before manifest success.

Purpose: Close RCIN-04 without allowing duplicate observations to overwrite each other or hashes to replace topology proof.
Output: Deterministic occurrence accounting, complete blockers, and structural/manifest composition.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-CONTEXT.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-RESEARCH.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-PATTERNS.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-02-SUMMARY.md
@src/release-candidate.ts
@tests/release-candidate.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify exact occurrence-ledger failures</name>
  <files>tests/release-candidate.test.ts</files>
  <behavior>
    - Final post-inspection candidate and source observation occurrences are retained separately and reconciled before either collection becomes a unique lookup.
    - Forward and reversed duplicate occurrences produce identical deterministic duplicate evidence and never collapse into one entry.
    - Missing, unexpected, wrong-root/path, wrong-kind, and absent integrity observations block independently with exact counts and safe paths.
    - Empty directories are reconciled structurally and excluded from manifest counts; a missing empty directory still blocks.
    - No manifest or digest is returned as passed when either the inclusion ledger or structural reconciliation fails.
  </behavior>
  <action>Add fixture-returned candidate observation arrays that contain exact duplicates, mixed duplicate/unexpected identities, omissions, wrong provenance/path, wrong kind/unobserved facts, and shuffled order. Assert `expectedFileCount`, raw `observedFileCount`, `matchedFileCount`, complete sorted blocker categories, and no truncation. Retain a separate empty-directory removal case to prove tree reconciliation is still mandatory. RED command: `npm test -- tests/release-candidate.test.ts -t "rejects non-bijective candidate integrity ledgers"`. Expected RED: duplicate entries are overwritten or malformed ledgers reach manifest success; unexpected GREEN requires stopping. Commit tests as `test(04-03): require exact candidate ledger`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "rejects non-bijective candidate integrity ledgers"</automated>
  </verify>
  <done>The focused matrix fails for absent occurrence accounting or improper structural/manifest composition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement occurrence-first inclusion reconciliation</name>
  <files>src/release-candidate.ts</files>
  <behavior>
    - Expected file identities come from accepted inventory; every strict candidate observation remains in a raw occurrence list until counts are complete.
    - Deterministic blockers cover every missing, duplicate, unexpected, wrong, or unobserved identity before a unique map is created.
    - Manifest construction proceeds only after both the file ledger and complete file/directory structural reconciliation pass.
  </behavior>
  <action>Build the expected file ledger from inventory regular files and retain every parsed final post-inspection candidate and source observation occurrence in separate ordinal collections. Count exact `(root,path)` occurrences before constructing any `Map` or `Set`; compare expected, final candidate, and final source multisets and emit complete deterministic blockers with sanitized evidence only after internal identities are reconciled. Preserve existing `reconcileCandidateTree` for files, fixed prefixes, and empty directories as a separate required predicate. Do not truncate evidence, consume pre-callback snapshots, or infer kind/mapping from hashes. Make the matrix GREEN and run candidate regressions, typecheck, and build. Commit production code as `feat(04-03): reconcile candidate inclusion ledger`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "rejects non-bijective candidate integrity ledgers" &amp;&amp; npm test -- tests/release-candidate.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>Every accepted file has exactly one observed manifest identity, all discrepancies are complete and deterministic, and empty-directory topology remains enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Adapter observation collection to unique identity lookup | Duplicate or unexpected entries can be hidden by lossy data structures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-06 | Tampering | candidate inclusion ledger | mitigate | Count every occurrence before building unique maps. |
| T-04-07 | Repudiation | file versus directory proof | mitigate | Require independent ledger and structural reconciliation predicates. |
</threat_model>

<verification>
Run the focused ledger matrix, complete candidate suite, typecheck, build, `git diff --check`, and staged-path inspection.
</verification>

<success_criteria>
- RED precedes GREEN with atomic commits.
- Duplicate, missing, unexpected, wrong, and unobserved entries cannot reach manifest success.
- Empty-directory reconciliation remains compatible with Phase 3 behavior.
</success_criteria>

<output>
Create `.planning/phases/04-integrity-manifest-and-verified-cleanup/04-03-SUMMARY.md` when done.
</output>
