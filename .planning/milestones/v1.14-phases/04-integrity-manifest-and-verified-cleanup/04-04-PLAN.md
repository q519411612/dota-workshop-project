---
phase: 04-integrity-manifest-and-verified-cleanup
plan: 04
type: tdd
wave: 4
depends_on: ["04-03"]
files_modified:
  - tests/release-readiness.test.ts
  - tests/release-candidate.test.ts
  - src/release-readiness.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCIN-05
must_haves:
  truths:
    - "Users receive exact counts and complete safe relative-path lists for fully scanned text, included binary, unreadable, and oversized files."
    - "Every legitimate binary, unreadable, and oversized regular file remains included in the integrity ledger and manifest; scan classification never controls inclusion."
    - "Unreadable, invalid-UTF-8, or oversized required text blocks artifact validation, while optional unscannable text remains explicit evidence."
  artifacts:
    - path: "src/release-readiness.ts"
      provides: "Deterministic scan coverage policy using the shared text and sensitive-material authority"
    - path: "src/release-candidate.ts"
      provides: "Identity-bound scan observations composed separately from inclusion and hashing"
    - path: "tests/release-readiness.test.ts"
      provides: "Pure coverage ordering, classification, and required-text policy tests"
    - path: "tests/release-candidate.test.ts"
      provides: "Integrated text/binary/unreadable/oversized/invalid-UTF-8 fixture matrix"
  key_links:
    - from: "accepted regular-file inventory"
      to: "scan coverage"
      via: "exactly one coverage state per file after inclusion remains fixed"
      pattern: "text|binary|unreadable|oversized"
    - from: "identity-bound scan observations"
      to: "shared readiness findings"
      via: "fatal UTF-8 decode, required-text policy, sensitive matching, and shared sanitizer"
      pattern: "evaluateReleaseReadiness|sanitizeRelativeEvidenceIdentity"
---

<objective>
Report complete scan coverage without confusing text scanning with candidate inclusion.

Purpose: Close RCIN-05 while preserving binary bytes and blocking required text that cannot be fully and safely scanned.
Output: Versioned scan states, deterministic coverage evidence, fatal UTF-8 handling, and shared-policy integration.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-CONTEXT.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-RESEARCH.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-PATTERNS.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-03-SUMMARY.md
@src/release-readiness.ts
@src/release-candidate.ts
@tests/release-readiness.test.ts
@tests/release-candidate.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify exhaustive scan coverage and required-text blocking</name>
  <files>tests/release-readiness.test.ts, tests/release-candidate.test.ts</files>
  <behavior>
    - Mixed fixtures report exact total counts and sorted sanitized lists for text, binary, unreadable, and oversized states with one state per accepted file.
    - Binary files remain hashed and manifested without decode attempts; optional unreadable/oversized text remains included with explicit warning evidence.
    - Required unreadable, invalid-encoding, or oversized text blocks; valid text is fatal-decoded and scanned by the existing sensitive policy.
    - Malformed scan states, getters, proxies, thenables, raw errors, private absolute paths, and matched secret values never enter evidence.
  </behavior>
  <action>Add pure readiness coverage cases and an integrated macOS fixture containing valid UTF-8 text, arbitrary binary, optional unreadable/oversized text, required unreadable/oversized text, invalid UTF-8, and sensitive text. Assert complete exact counts/lists, ordinal ordering before redaction, no evidence truncation, manifest inclusion for every regular file, and no decode call for binary. RED command: `npm test -- tests/release-readiness.test.ts tests/release-candidate.test.ts -t "reports complete release candidate scan coverage"`. Expected RED: current reads cannot represent complete unreadable/invalid-encoding coverage and no explicit four-class aggregate exists; unexpected GREEN requires stopping. Commit tests as `test(04-04): require complete scan coverage`.</action>
  <verify>
    <automated>npm test -- tests/release-readiness.test.ts tests/release-candidate.test.ts -t "reports complete release candidate scan coverage"</automated>
  </verify>
  <done>The coverage matrix fails for absent four-class evidence or for accepting unscannable required text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded scan observations and coverage policy</name>
  <files>src/release-readiness.ts, src/release-candidate.ts</files>
  <behavior>
    - Identity-bound scan observations distinguish readable text, binary, unreadable/invalid encoding, and oversized without returning raw exceptions or entire oversized files.
    - Coverage is deterministic, exhaustive, sanitized, and independent of manifest inclusion.
    - Required text is fully read within the explicit bound and decoded with fatal UTF-8 semantics before sensitive scanning.
  </behavior>
  <action>Version and broaden the accepted-file scan observation contract so the adapter can return strict readable, binary, unreadable/invalid-encoding, and oversized states. Use `isReleaseTextPath` only for scan policy, never inclusion; binary files bypass decoding but remain in the hash ledger. Use fatal `TextDecoder` behavior for text bytes, preserve `MAX_SECRET_SCAN_BYTES`, required-path policy, sensitive classifiers, deterministic finding order, and `sanitizeRelativeEvidenceIdentity`. Produce exact counts plus complete sorted safe lists for all four public coverage categories. Malformed observations remain adapter-contract failures. Make focused tests GREEN and run readiness/preflight/source-snapshot/candidate regressions, typecheck, and build. Commit both production files together as `feat(04-04): report candidate scan coverage` because the adapter state and policy aggregate form one contract.</action>
  <verify>
    <automated>npm test -- tests/release-readiness.test.ts tests/release-candidate.test.ts -t "reports complete release candidate scan coverage" &amp;&amp; npm test -- tests/release-readiness.test.ts tests/release-candidate.test.ts tests/preflight.test.ts tests/source-snapshot.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>Every accepted file has one explicit coverage class, binaries remain included, and all unscannable required text blocks with sanitized evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| File bytes to text/sensitive scan policy | Invalid encoding, oversized input, or secret-bearing content can be misclassified or leaked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-08 | Information Disclosure | scan findings | mitigate | Reuse shared sanitizer and categories; never return matched values or raw errors. |
| T-04-09 | Denial of Service | text scanning | mitigate | Enforce explicit size bound while hashing all files as streams. |
| T-04-10 | Tampering | binary/text classification | mitigate | Keep classification independent from inclusion and block unscannable required text. |
</threat_model>

<verification>
Run focused and regression suites, typecheck, build, `git diff --check`, and confirm no source/candidate repair or persistent artifact behavior exists.
</verification>

<success_criteria>
- RED precedes GREEN with atomic test and production commits.
- Coverage accounts for every accepted file exactly once with complete sanitized evidence.
- Binary inclusion and required-text blocking remain explicit and independently tested.
</success_criteria>

<output>
Create `.planning/phases/04-integrity-manifest-and-verified-cleanup/04-04-SUMMARY.md` when done.
</output>
