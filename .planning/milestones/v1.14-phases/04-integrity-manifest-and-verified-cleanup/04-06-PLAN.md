---
phase: 04-integrity-manifest-and-verified-cleanup
plan: 06
type: tdd
wave: 6
depends_on: ["04-05"]
files_modified:
  - tests/release-candidate.test.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCCL-02
must_haves:
  truths:
    - "Users can distinguish overall operation success, artifact-validation state, and cleanup evidence in every lifecycle result."
    - "Cleanup failure, malformed/unknown cleanup, identity mismatch, removal failure, or missing absence proof always forces overall ok false."
    - "A previously passed artifact manifest and coverage state remains observable after cleanup failure, while callback value and usable candidate path are withheld."
    - "Callback mutation, including mutation immediately before a throw, is caught by fresh candidate and source-after observations before cleanup and can never leave stale passed artifact evidence."
    - "Artifact validation passes only when the final triple comparison, canonical manifest, exact occurrence ledger, and scan coverage from 04-01 through 04-04 all pass together."
  artifacts:
    - path: "src/release-candidate.ts"
      provides: "Immutable artifact-validation and cleanup domains with explicit overall precedence"
    - path: "tests/release-candidate.test.ts"
      provides: "Artifact/operation/cleanup cross-product and hostile cleanup contract tests"
  key_links:
    - from: "manifest, ledger, scan, and blocker outcomes"
      to: "artifactValidation"
      via: "composition with the final post-inspection triple outcome into passed, blocked, or not-reached state"
      pattern: "artifactValidation"
    - from: "artifactValidation and cleanup evidence"
      to: "overall ok/value"
      via: "explicit conjunction and stale-value withholding"
      pattern: "cleanup|ok"
    - from: "callback success or failure"
      to: "final artifact state"
      via: "post-inspection candidate/source observations, occurrence ledger, triple equality, and manifest validation before exactly-once cleanup"
      pattern: "artifactValidation|cleanupCandidateLease"
---

<objective>
Preserve artifact truth while making verified cleanup a non-negotiable condition of overall success.

Purpose: Close RCCL-02 and finish the internal Phase 4 lifecycle without exposing Phase 5 contracts.
Output: Separate result domains, strict cleanup parsing, precedence matrix, and complete macOS quality-gate evidence.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-CONTEXT.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-RESEARCH.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-PATTERNS.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-05-SUMMARY.md
@.planning/phases/03-safe-candidate-assembly/03-VERIFICATION.md
@src/release-candidate.ts
@tests/release-candidate.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify artifact and cleanup precedence independently</name>
  <files>tests/release-candidate.test.ts</files>
  <behavior>
    - Artifact states `not-reached`, `blocked`, and `passed` remain distinct from operation/callback outcome and cleanup state.
    - Final triple equality, canonical manifest, exact occurrence ledger, and exhaustive coverage are all required inputs to `artifactValidation: passed`.
    - Cleanup verifies only when its supported version, lease identity match, removal, and post-cleanup absence facts are literal true with exactly one attempt.
    - Cleanup false/unknown/malformed/getter/Proxy/thenable/throw cases force `ok: false` while preserving a passed artifact manifest and coverage.
    - Callback mutation and mutation-before-throw are detected by fresh post-callback candidate plus source-after observations; final artifact state is blocked with a stable integrity code, never stale passed.
    - Any overall failure omits callback value and candidate path; serialized evidence contains no stale path, raw exception, private root, or secret.
  </behavior>
  <action>Add a full cross-product table for final triple/manifest/ledger/coverage composition, artifact passed/blocked/not-reached, callback success/failure, and cleanup verified/failed/unknown. Include identity mismatch, removed false, absent false/missing, unsupported version, unsafe attempt count, getters, proxies, thenables, and thrown cleanup. Inject candidate byte mutation during a successful callback and immediately before a callback throw; assert fresh lease-bound candidate and source-after observations occur after callback settlement and before cleanup, then the 04-01 triple outcome, 04-02 canonical manifest, 04-03 occurrence ledger, and 04-04 coverage are composed exactly once into a stable blocked artifact state. Assert no stale passed evidence, callback value withheld, cleanup state separately observable, and cleanup called exactly once. Also assert immutable separate domains, preserved genuinely passed manifest/coverage when cleanup alone fails, no candidate path, and unchanged source snapshots. RED command: `npm test -- tests/release-candidate.test.ts -t "preserves final artifact truth across callback and cleanup failures"`. Expected RED: cleanup can replace artifact state or stale pre-callback evidence can survive callback mutation/throw; unexpected GREEN requires stopping. Commit tests as `test(04-06): require final artifact cleanup precedence`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "preserves final artifact truth across callback and cleanup failures"</automated>
  </verify>
  <done>The cross-product fails on outcome replacement, permissive cleanup proof, missing final source/candidate observation, stale artifact success, or stale value/path exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement separate result domains and run all gates</name>
  <files>src/release-candidate.ts</files>
  <behavior>
    - Artifact validation is immutable and records not-reached, blocked, or passed with its manifest/coverage evidence where applicable.
    - Cleanup evidence separately records attempted/attempts/status and strict identity/removal/absence proof or a stable failure category.
    - Overall success is true only when the operation/callback completed, artifact validation passed, and cleanup verified; value is returned only then.
    - Final artifact state is computed after callback settlement from fresh candidate/source observations and cannot reuse provisional pre-callback manifest evidence.
  </behavior>
  <action>Replace cleanup-as-lifecycle-blocker overwriting with separate immutable `artifactValidation` and `cleanup` discriminated domains. Consume the final post-callback triple outcome from 04-01, canonical manifest from 04-02, exact occurrence ledger from 04-03, and exhaustive coverage from 04-04; set `artifactValidation: passed` only when all four final domains pass together. A callback mutation or mutation-before-throw records a stable blocked artifact state and never reuses pre-callback evidence; a callback throw remains an operation failure even if final bytes are unchanged. Parse every cleanup property inside one guarded normalization boundary and accept verified cleanup only for the fixed version, one attempt, identity matched, removal completed, and post-cleanup absence proven. Compute overall `ok` as the explicit conjunction of operation completion, final artifact passed, and cleanup verified. Preserve genuinely passed final manifest/coverage when cleanup alone fails, but omit callback value and candidate root on every overall failure. Cleanup executes exactly once after final evidence composition regardless of outcome. Keep stable existing Phase 3 blocker semantics where structurally compatible. Make the focused and full suites GREEN, then run every verification command below and independently review final observation ordering, hashing, occurrence accounting, scan coverage, cleanup paths, redaction, source immutability, and scope. Commit production code as `feat(04-06): separate artifact and cleanup states` only after focused gates pass.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "preserves final artifact truth across callback and cleanup failures" &amp;&amp; npm test -- tests/release-candidate.test.ts tests/release-readiness.test.ts tests/preflight.test.ts tests/source-snapshot.test.ts tests/install-simulation.test.ts tests/result.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>Final post-callback artifact truth survives cleanup failure, callback mutation cannot retain stale success, cleanup uncertainty always forces overall failure, and the complete Phase 4 gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Cleanup adapter result to caller-visible success | Narration, malformed facts, or stale candidate values could falsely imply safe cleanup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-13 | Spoofing | cleanup proof | mitigate | Require exact version, identity match, removal, absence, and one attempt. |
| T-04-14 | Repudiation | artifact versus cleanup outcome | mitigate | Preserve separate immutable domains and explicit precedence. |
| T-04-15 | Information Disclosure | failed lifecycle value/evidence | mitigate | Withhold callback value/path and sanitize all public evidence. |
</threat_model>

<verification>
Run the focused callback success/throw/mutation/mutation-before-throw matrix first, then in order: `npm test -- tests/release-candidate.test.ts tests/release-readiness.test.ts tests/preflight.test.ts tests/source-snapshot.test.ts tests/install-simulation.test.ts tests/result.test.ts`; `npm test`; `npm run typecheck`; `npm run build`; `npm run verify:plugin`; `npm run verify:source-snapshot`; `npm run verify:install-simulation`; `npm run verify:rc`; `npm run verify:handoff`; `npm run verify:milestone`; `git diff --check`. Inspect `git status --short` and `git diff --cached --name-only`; preserve and exclude `.planning/graphs/`. Perform an independent review of final post-inspection observation ordering, provisional-versus-committed manifest state, streaming/no-buffer integrity, strict triple equality, ordinal manifest serialization, occurrence-first ledgers, all four scan categories, exactly-once cleanup, artifact/cleanup precedence, hostile normalization, redaction, source immutability, and Phase 5/prohibited boundaries.
</verification>

<success_criteria>
- RED precedes GREEN with atomic test and production commits.
- RCIN-01 through RCIN-05 and RCCL-01 through RCCL-02 each have passing focused and full-suite evidence.
- Cleanup unknown/failure always makes overall success false without erasing artifact-validation truth.
- Callback mutation and mutation-before-throw fail final artifact validation after fresh candidate/source observations, with callback value withheld and cleanup attempted once.
- No public MCP/schema/server/remote/docs integration, credential handling, Steam/Workshop mutation, upload, persistent archive, signing, encryption, compilation, repair, retention, or real-Windows claim exists.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Exact stable-source byte evidence and proof no temporary candidate remains | 01-06 | COVERED | Integrity, manifest, ledger, coverage, cleanup ownership, and precedence compose the goal. |
| REQ | RCIN-01 | Source-before/source-after/candidate SHA-256 equality | 01 | COVERED | Identity-bound streaming triple comparison. |
| REQ | RCIN-02 | Versioned ordinal per-file manifest | 02 | COVERED | Fixed fields and inventory provenance. |
| REQ | RCIN-03 | Host-independent canonical combined digest | 02 | COVERED | Fixed nested-array UTF-8 serialization. |
| REQ | RCIN-04 | Exact one-to-one inclusion ledger | 03 | COVERED | Occurrence count before maps plus topology reconciliation. |
| REQ | RCIN-05 | Text/binary/unreadable/oversized coverage | 04 | COVERED | Exhaustive scan states independent from inclusion. |
| REQ | RCCL-01 | Cleanup evidence after every post-create outcome | 05 | COVERED | Strict acquisition ownership and one cleanup funnel. |
| REQ | RCCL-02 | Cleanup uncertainty forces false while artifact state remains | 06 | COVERED | Separate domains and explicit precedence. |
| RESEARCH | — | Streaming identity-bound observations; no whole-file buffering | 01 | COVERED | Adapter-owned chunk hashing and exact byte count. |
| RESEARCH | — | Fixed versioned canonical manifest; no locale/host fields | 02 | COVERED | Known vectors and permutations. |
| RESEARCH | — | Occurrence ledger before map; preserve empty directories | 03 | COVERED | Independent file and tree predicates. |
| RESEARCH | — | Fatal UTF-8 and four-class scan evidence | 04 | COVERED | Shared readiness authority and sanitizer. |
| RESEARCH | — | Malformed acquisition and all post-create exits own cleanup | 05 | COVERED | Created-state acquisition contract and exactly-once tests. |
| RESEARCH | — | Artifact/operation/cleanup state machine | 06 | COVERED | Cleanup failure cannot replace artifact outcome. |
| CONTEXT | — | No retry, recopy, repair, truncation, raw paths, exceptions, or matched secrets | 01-06 | COVERED | Explicit task constraints and adversarial tests. |
| CONTEXT | — | Fixture/macOS adapter-contract completion only | 01-06 | COVERED | No real Windows assertion or host fallback. |
| CONTEXT | deferred | Public MCP/schema/server/remote/docs integration and target parity | Phase 5 | EXCLUDED | Correctly outside Phase 4. |
| CONTEXT | deferred | Steam/Workshop mutation, credentials, archive, signing, encryption, compile, repair, transfer, retention | Outside v1.14 | EXCLUDED | Prohibited throughout every plan. |
</source_audit>

<output>
Create `.planning/phases/04-integrity-manifest-and-verified-cleanup/04-06-SUMMARY.md` when done.
</output>
