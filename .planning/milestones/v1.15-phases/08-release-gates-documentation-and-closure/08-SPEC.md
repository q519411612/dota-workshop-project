# Phase 8 Spec: Release Gates, Documentation, and Closure

**Goal:** Close v1.15 with complete adversarial evidence, packaged runtime parity, operator guidance, independent review, and audit traceability.

## Ambiguity Report

Goal 0.99, boundary 0.98, constraint 0.96, acceptance 0.98. **Weighted ambiguity:** 0.02.

## Acceptance Criteria

- Required macOS fixture failure classes and four-target contract parity are explicit tests.
- All existing behavior, especially temporary preflight cleanup, remains green.
- Examples, README, skill guidance, runbook, packaged runtime, and verification scripts cover both operations and boundaries.
- Every quality gate passes from tracked source.
- Independent review findings are reproduced and fixed before audit.
- Real Windows evidence is sanitized if available; otherwise the audit says unverified.

