---
wave: 1
depends_on: []
requirements_addressed: [UABL2-01, UABL2-02, UABL2-03, UABL2-04, UABL2-05, UABL2-06, UABL2-07, UABL2-08]
autonomous: true
---

# Phase 12 Plan: Unit Ability Scaffolding MVP

## Objective

Add minimal custom unit plus linked custom ability KV scaffolding while preserving existing runtime smoke behavior.

## Tasks

<task id="12-01-T1" type="tdd">
<title>Write failing scaffold tests</title>
<read_first>
- `tests/addon.test.ts`
- `tests/remote-operations.test.ts`
- `tests/smoke.test.ts`
- `src/addon.ts`
- `src/schemas.ts`
- `src/smoke.ts`
</read_first>
<action>
Add tests for `unitAbilityScaffold` schema parsing, generated unit KV, generated ability KV, invalid scaffold rejection before writes, inspect scaffold evidence, remote command construction, remote invalid scaffold command suppression, and scaffolded smoke marker compatibility.
</action>
<acceptance_criteria>
- `npx vitest run tests/addon.test.ts tests/remote-operations.test.ts tests/smoke.test.ts` fails before implementation with missing `unitAbilityScaffold` behavior.
- The failing assertions name `npc_dota_workshop_mcp_dummy`, `ability_dota_workshop_mcp_dummy`, `npc_abilities_custom.txt`, and `INVALID_SCAFFOLD`.
</acceptance_criteria>
</task>

<task id="12-01-T2" type="implementation">
<title>Add scaffold renderer and validation</title>
<read_first>
- `src/addon.ts`
- `src/remote.ts`
- `src/schemas.ts`
- `src/server.ts`
- `src/smoke.ts`
</read_first>
<action>
Add `UnitAbilityScaffold`, `validateUnitAbilityScaffold`, renderer support for `npc_units_custom.txt` and `npc_abilities_custom.txt`, local and remote write support, schema/server input exposure, smoke passthrough, and inspect evidence. Keep `run_playable_smoke` expected marker composition unchanged for scaffold input.
</action>
<acceptance_criteria>
- `src/addon.ts` exports `UnitAbilityScaffold` and `validateUnitAbilityScaffold`.
- `renderAddonFiles` returns `abilityData`.
- `addonPaths` includes `abilityData`.
- `createAddon` and `createRemoteAddon` reject invalid scaffold names before writes or remote command construction.
- `runPlayableSmoke` passes `unitAbilityScaffold` into addon creation without adding scaffold markers.
</acceptance_criteria>
</task>

<task id="12-01-T3" type="documentation">
<title>Document scaffold scope and limits</title>
<read_first>
- `README.md`
- `skills/dota2-workshop-tools/SKILL.md`
- `skills/dota2-workshop-tools/references/minimal-template.md`
- `skills/dota2-workshop-tools/references/addon-layout.md`
- `skills/dota2-workshop-tools/references/troubleshooting.md`
</read_first>
<action>
Document `unitAbilityScaffold`, generated `npc_units_custom.txt` and `npc_abilities_custom.txt` content, inspect evidence, and the boundary that this phase does not prove Lua ability behavior, items, heroes, AI, UI, or publishing.
</action>
<acceptance_criteria>
- README mentions `unitAbilityScaffold`.
- Skill references mention generated ability support file and deferred runtime ability behavior.
- Troubleshooting explains scaffold evidence separately from runtime marker validation.
</acceptance_criteria>
</task>

<task id="12-01-T4" type="verification">
<title>Verify locally and on real Windows</title>
<read_first>
- `.planning/phases/12-unit-ability-scaffolding-mvp/12-SPEC.md`
- `package.json`
- `src/addon.ts`
- `src/smoke.ts`
</read_first>
<action>
Run local verification, build dist output, run manifest/reference checks, run secret scan, and run real Windows scaffolded playable smoke through the remote target without storing private target details in the repository.
</action>
<acceptance_criteria>
- `git diff --check` exits 0.
- `npm run typecheck` exits 0.
- `npm test` exits 0.
- `npm run build` exits 0.
- Secret scan reports no private host, password, token, or key material.
- Real Windows smoke validates existing playable markers from `game/dota/console.log` for an addon created with `unitAbilityScaffold`.
</acceptance_criteria>
</task>

<task id="12-01-T5" type="review">
<title>Perform independent review and close artifacts</title>
<read_first>
- `src/addon.ts`
- `src/remote.ts`
- `src/smoke.ts`
- `tests/addon.test.ts`
- `tests/remote-operations.test.ts`
- `tests/smoke.test.ts`
- `.planning/phases/12-unit-ability-scaffolding-mvp/12-SPEC.md`
</read_first>
<action>
Review for invalid-input leaks, remote/local renderer drift, default smoke marker drift, documentation overclaiming, and credential hygiene. Write verification, review, and summary artifacts, then update roadmap, requirements, project, and state to completed status.
</action>
<acceptance_criteria>
- `12-VERIFICATION.md` records local and Windows evidence.
- `12-REVIEW.md` records findings and residual risk.
- `12-01-SUMMARY.md` records delivered scope and next work.
- ROADMAP, REQUIREMENTS, PROJECT, and STATE point to the next slice after scaffold completion.
</acceptance_criteria>
</task>

## Artifacts This Phase Produces

- `UnitAbilityScaffold`
- `validateUnitAbilityScaffold`
- `unitAbilityScaffold` input field
- `abilityData` rendered file field
- `scripts/npc/npc_abilities_custom.txt`
- Fixture tests for scaffold generation and validation
- Remote command tests for scaffold parity
- Smoke tests for scaffold compatibility

## Must Haves

truths:
- `unitAbilityScaffold.unitName` must be validated before local writes and remote command construction.
- `unitAbilityScaffold.abilityName` must be validated before local writes and remote command construction.
- Generated unit KV must link `Ability1` to the requested ability.
- Generated ability KV must exist in `npc_abilities_custom.txt`.
- Scaffolded smoke must not add runtime marker expectations.

prohibitions:
- statement: Must not claim custom ability runtime execution from scaffold generation alone.
  status: resolved
  verification: judgment
- statement: Must not silently fall back to empty scaffold files on invalid names.
  status: resolved
  verification: test
- statement: Must not introduce a remote-only scaffold renderer.
  status: resolved
  verification: test
- statement: Must not change default smoke expected markers when scaffold input is absent.
  status: resolved
  verification: test
- statement: Must not store private remote host, account, password, token, or Steam credentials.
  status: resolved
  verification: test

## Verification Commands

- `npx vitest run tests/addon.test.ts tests/remote-operations.test.ts tests/smoke.test.ts`
- `git diff --check`
- `npm run typecheck`
- `npm test`
- `npm run build`
- Plugin manifest validation command from prior phases
- Strict secret scan from prior phases
- Real Windows scaffolded playable smoke with runtime marker validation
