---
wave: 1
depends_on: []
requirements_addressed: [PRE2-01, PRE2-02, PRE2-03, PRE2-04, PRE2-05, PRE2-06, PRE2-07, PRE2-08]
autonomous: true
---

# Phase 13 Plan: Panorama Toolchain Boundary and Publishing Preflight MVP

## Objective

Add an inspection-only Workshop preflight operation that reports addon layout, Panorama boundary, toolchain marker, and publishing blocker evidence without generating, building, encrypting, uploading, or storing credentials.

## Tasks

<task id="13-01-T1" type="tdd">
<title>Write failing preflight contract tests</title>
<read_first>
- `tests/addon.test.ts`
- `tests/remote-operations.test.ts`
- `tests/smoke.test.ts`
- `src/schemas.ts`
- `src/tools.ts`
</read_first>
<action>
Add tests for `inspect_workshop_preflight` schema parsing, tool-name exposure, dispatcher routing, fixture layout evidence, Panorama absent/present evidence, toolchain marker evidence, publishing blocker warnings, invalid input rejection, remote command construction, and invalid remote command suppression.
</action>
<acceptance_criteria>
- `npx vitest run tests/preflight.test.ts tests/remote-operations.test.ts` fails before implementation because the schema/tool/preflight behavior does not exist.
- Failing assertions name `inspect_workshop_preflight`, Panorama evidence, toolchain marker evidence, publishing blockers, and invalid remote command suppression.
</acceptance_criteria>
</task>

<task id="13-01-T2" type="implementation">
<title>Add local preflight inspection</title>
<read_first>
- `src/addon.ts`
- `src/result.ts`
- `src/types.ts`
- `src/schemas.ts`
</read_first>
<action>
Add local/fixture preflight support that validates addon name and target root, checks deterministic addon layout paths, reports Panorama directory/file evidence, detects toolchain marker files without executing tools, and returns publishing blocker warnings.
</action>
<acceptance_criteria>
- Invalid addon names and missing roots fail before reads.
- Existing generated addon files produce present evidence.
- Missing optional Panorama/toolchain files produce explicit evidence or warnings.
- Publishing blockers are always explicit and do not require credential input.
</acceptance_criteria>
</task>

<task id="13-01-T3" type="implementation">
<title>Add remote preflight parity</title>
<read_first>
- `src/remote.ts`
- `src/tools.ts`
- `src/server.ts`
- `src/schemas.ts`
- `tests/remote-operations.test.ts`
</read_first>
<action>
Add remote preflight command construction and parsing through the existing remote adapter. Expose the tool through schemas, dispatcher, server registration, and tool names.
</action>
<acceptance_criteria>
- Remote preflight validates input before command construction.
- Remote command checks the same evidence categories as local preflight.
- Parsed remote payload becomes the same `ToolResult` shape.
- Server and dispatcher expose `inspect_workshop_preflight`.
</acceptance_criteria>
</task>

<task id="13-01-T4" type="documentation">
<title>Document preflight boundaries</title>
<read_first>
- `README.md`
- `skills/dota2-workshop-tools/SKILL.md`
- `skills/dota2-workshop-tools/references/addon-layout.md`
- `skills/dota2-workshop-tools/references/troubleshooting.md`
</read_first>
<action>
Document `inspect_workshop_preflight`, its evidence categories, publishing blocker warnings, and the strict boundary that it does not generate UI, run toolchains, encrypt, upload, or store credentials.
</action>
<acceptance_criteria>
- README mentions `inspect_workshop_preflight`.
- Skill references explain when to use preflight.
- Docs keep React Panorama, TypeScript-to-Lua, publishing, encryption, and credential handling deferred.
</acceptance_criteria>
</task>

<task id="13-01-T5" type="verification">
<title>Verify locally and with remote Windows preflight</title>
<read_first>
- `.planning/phases/13-panorama-toolchain-boundary-and-publishing-preflight-mvp/13-SPEC.md`
- `package.json`
- `src/preflight.ts`
- `src/remote.ts`
</read_first>
<action>
Run local verification, build dist output, run manifest/reference checks, run secret scan, refresh graphify if code or planning graph is stale, and run real Windows remote preflight against a generated addon tree without storing private target details in the repository.
</action>
<acceptance_criteria>
- `git diff --check` exits 0.
- `npm run typecheck` exits 0.
- `npm test` exits 0.
- `npm run build` exits 0.
- Secret scan reports no private host, password, token, key, Steam credential, or upload credential material.
- Real Windows remote preflight returns addon layout, Panorama/toolchain, and publishing blocker evidence.
</acceptance_criteria>
</task>

<task id="13-01-T6" type="review">
<title>Perform independent review and close artifacts</title>
<read_first>
- `src/preflight.ts`
- `src/remote.ts`
- `src/tools.ts`
- `src/schemas.ts`
- `tests/preflight.test.ts`
- `tests/remote-operations.test.ts`
- `.planning/phases/13-panorama-toolchain-boundary-and-publishing-preflight-mvp/13-SPEC.md`
</read_first>
<action>
Review for credential leakage, accidental generation/build/upload behavior, local/remote evidence drift, missing root handling, docs overclaiming, and runtime-validation confusion. Write verification, review, and summary artifacts, update planning docs, then commit and push.
</action>
<acceptance_criteria>
- `13-VERIFICATION.md` records local and Windows evidence.
- `13-REVIEW.md` records findings and residual risk.
- `13-01-SUMMARY.md` records delivered scope and next work.
- ROADMAP, REQUIREMENTS, PROJECT, and STATE reflect v2.7 completion and the next slice.
</acceptance_criteria>
</task>

## Artifacts This Phase Produces

- `src/preflight.ts`
- `InspectWorkshopPreflightInputSchema`
- `inspect_workshop_preflight` MCP tool
- Fixture tests for layout, Panorama, toolchain, and publishing blocker evidence
- Remote command tests for preflight parity
- README and skill reference updates

## Must Haves

truths:
- Preflight must validate addon names before filesystem reads or remote command construction.
- Preflight must return explicit evidence for missing optional directories and files.
- Preflight must report Panorama/toolchain evidence without generating or running anything.
- Preflight must report publishing blockers without accepting credentials.
- Remote and local preflight must share logical evidence categories.

prohibitions:
- statement: Must not generate Panorama UI files.
  status: resolved
  verification: test
- statement: Must not run TypeScript-to-Lua, React, npm, bundlers, or compilers.
  status: resolved
  verification: test
- statement: Must not accept or store Steam credentials, publishing keys, remote credentials, or private host data.
  status: resolved
  verification: test
- statement: Must not upload to Workshop or encrypt content.
  status: resolved
  verification: judgment
- statement: Must not treat preflight success as runtime validation success.
  status: resolved
  verification: judgment
- statement: Must not silently mark missing files as publish-ready.
  status: resolved
  verification: test

## Verification Commands

- `npx vitest run tests/preflight.test.ts tests/remote-operations.test.ts`
- `git diff --check`
- `npm run typecheck`
- `npm test`
- `npm run build`
- Plugin manifest validation command from prior phases
- Strict secret scan from prior phases
- Graphify status or rebuild when graph artifacts are stale
- Real Windows remote preflight inspection
