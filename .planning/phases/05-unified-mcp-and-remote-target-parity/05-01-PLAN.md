---
phase: 05-unified-mcp-and-remote-target-parity
plan: 01
type: tdd
wave: 4
depends_on: ["05-03", "05-05"]
files_modified:
  - src/tools.ts
  - src/server.ts
  - tests/preflight-release-candidate.test.ts
  - tests/preflight.test.ts
  - README.md
  - skills/dota2-workshop-tools/SKILL.md
autonomous: true
requirements:
  - RCOP-01
must_haves:
  truths:
    - "Users can discover and invoke exactly one new `preflight_release_candidate` operation through fixture, local, SSH, and PowerShell Remoting targets."
    - "The dispatcher validates the exact input schema and routes fixture/local to the production Node lifecycle and remote targets to the dedicated strict remote lifecycle."
    - "MCP text content and structuredContent serialize the same ToolResult, including the release-candidate detail on success and failure."
    - "The adjacent `dry_run_release_report` schema, route, result, warnings, blockers, paths, commands, logs, and inspection-only behavior remain byte-equivalent to their characterized baseline."
    - "The canonical tool list, server registration, README list, and skill list remain synchronized; runtime packaging is verified separately by 05-07."
  artifacts:
    - path: "src/tools.ts"
      provides: "Canonical tool name and target dispatcher for preflight_release_candidate"
    - path: "src/server.ts"
      provides: "MCP registration using the exact target plus addonName schema and structuredContent result"
    - path: "tests/preflight-release-candidate.test.ts"
      provides: "Discoverability, routing, structuredContent, schema rejection, and target-contract integration tests"
    - path: "tests/preflight.test.ts"
      provides: "Complete dry_run_release_report compatibility characterization"
  key_links:
    - from: "MCP preflight_release_candidate registration"
      to: "handleTool dispatcher"
      via: "asToolContent preserves identical text and structuredContent"
      pattern: "preflight_release_candidate|asToolContent"
    - from: "fixture/local target"
      to: "production Node lifecycle"
      via: "preflightNodeReleaseCandidate"
      pattern: "preflightNodeReleaseCandidate"
    - from: "remote target"
      to: "strict target-native remote lifecycle"
      via: "preflightRemoteReleaseCandidate with no local fallback"
      pattern: "preflightRemoteReleaseCandidate"
    - from: "toolNames"
      to: "README and skill documented lists"
      via: "plugin drift verifier"
      pattern: "README tool list matches toolNames|skill tool list matches toolNames"
---

<objective>
Expose the verified lifecycle through one additive source-level MCP operation without changing the existing dry-run report.

Purpose: Close RCOP-01 by wiring the exact public schema, target dispatch, MCP registration, structured result, discoverability lists, and packaged runtime only after local and remote services exist.
Output: Registered source operation, four-target routing, exact legacy compatibility evidence, and synchronized tool lists ready for 05-07 packaging.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-unified-mcp-and-remote-target-parity/05-CONTEXT.md
@.planning/phases/05-unified-mcp-and-remote-target-parity/05-RESEARCH.md
@.planning/phases/05-unified-mcp-and-remote-target-parity/05-03-SUMMARY.md
@.planning/phases/05-unified-mcp-and-remote-target-parity/05-05-SUMMARY.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-VERIFICATION.md
@src/schemas.ts
@src/tools.ts
@src/server.ts
@src/release-candidate-node.ts
@src/release-candidate-remote.ts
@tests/preflight.test.ts
@tests/plugin.test.ts
</context>

<execution_guard>
Phase preflight is owned only by the root orchestrator. Immediately before Wave 1 and before any Phase 5 edit or commit, it must create `/tmp/dota-workshop-phase5-graphs.sha256` from the three user-owned graph files and `/tmp/dota-workshop-phase5-start.head` from the exact phase-start `HEAD`, verify at creation time that the recorded commit equals the then-current `HEAD`, and make both files read-only. This plan must refuse to start unless both files already exist and contain valid data, the recorded commit exists and is an ancestor of current `HEAD`, current graph hashes equal the recorded baseline, and `git diff --cached --quiet -- .planning/graphs` succeeds. The plan and executor must never create, replace, refresh, or chmod either file. Repeat the same ancestor, graph-baseline, and cached-graph guards before every commit; fail closed on any missing file, mismatch, invalid marker, or staged graph change.
</execution_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit failing MCP surface and dry-run compatibility tests</name>
  <files>tests/preflight-release-candidate.test.ts, tests/preflight.test.ts</files>
  <behavior>
    - The canonical tool list and MCP server expose exactly one `preflight_release_candidate` operation with target plus addonName input.
    - Fixture and local dispatch call only the production Node service; SSH and PowerShell targets call only the strict remote service.
    - Input validation rejects missing/invalid addonName and all unknown or prohibited fields before any lifecycle/executor call.
    - `asToolContent` text parses to exactly the same object as structuredContent and sets isError from recomputed result success.
    - Existing representative dry-run success, metadata blocker, sensitive blocker, invalid addon, missing root, local, fixture, and remote result objects remain exact and the old route never creates a candidate.
    - Tool list documentation drift is detected.
    - Equivalent fixture/local and SSH/PowerShell service doubles preserve the same normalized detail through the public route, while only target-specific execution metadata differs.
  </behavior>
  <action>Add a new integration test file with injectable/counter-observable local and remote service seams or module-level fixtures following current tool tests. Assert schema, toolNames, server registration, dispatcher selection for all four target contracts, exact structuredContent parity, and responsibility-specific public-route parity for equivalent normalized local/remote details. Extend the existing dry-run whole-result characterization so any schema/route/result change or candidate-lifecycle call fails. Add tool-list drift assertions expected by plugin verification. Run focused tests and require failure because the tool is not registered/routed. Stop on unexpected GREEN. Run the graph comparison and cached-diff guard immediately before committing only tests as `test(05-01): require candidate preflight tool`.</action>
  <verify>
    <automated>npm test -- tests/preflight-release-candidate.test.ts tests/preflight.test.ts tests/result.test.ts tests/plugin.test.ts &amp;&amp; test -r /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test ! -w /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test -r /tmp/dota-workshop-phase5-start.head &amp;&amp; test ! -w /tmp/dota-workshop-phase5-start.head &amp;&amp; PHASE5_START_HEAD=\"$(cat /tmp/dota-workshop-phase5-start.head)\" &amp;&amp; test -n \"$PHASE5_START_HEAD\" &amp;&amp; git cat-file -e \"${PHASE5_START_HEAD}^{commit}\" &amp;&amp; git merge-base --is-ancestor \"$PHASE5_START_HEAD\" HEAD &amp;&amp; (cd .planning/graphs &amp;&amp; shasum -a 256 GRAPH_REPORT.md graph.html graph.json &gt; /tmp/dota-workshop-phase5-graphs.current.sha256) &amp;&amp; cmp -s /tmp/dota-workshop-phase5-graphs.sha256 /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; rm /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; git diff --cached --quiet -- .planning/graphs</automated>
  </verify>
  <done>The atomic RED commit fails on missing public tool/route while proving exact legacy dry-run compatibility and structuredContent requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register and dispatch the additive source operation</name>
  <files>src/tools.ts, src/server.ts, README.md, skills/dota2-workshop-tools/SKILL.md</files>
  <behavior>
    - The new operation is additive and selects the correct already-tested target service.
    - Server input and dispatcher parsing use the exact strict schema and return the common ToolResult through asToolContent.
    - README and skill canonical tool lists include the operation exactly once.
    - Source integration remains loadable under tests and declares the exact imports that 05-07 must package.
  </behavior>
  <action>Import the exact schema and inferred type, append the new name once to `toolNames`, and add one dispatcher case. Route fixture/local to `preflightNodeReleaseCandidate`; route remote to `preflightRemoteReleaseCandidate`. Register the MCP tool with a temporary evidence-only description and the exact target/addonName schema; do not duplicate schemas with extra fields. Keep the existing dry-run case untouched except for import ordering required by formatting. Update only the canonical README and skill tool-list entries needed for plugin drift gates; broader workflow guidance belongs to 05-06. Do not promote or commit generated runtime output in this plan. Make focused tests GREEN, run the graph comparison and cached-diff guard, and commit source/tests/docs as `feat(05-01): expose candidate preflight`; 05-07 owns executable runtime synchronization after proving the required import closure.</action>
  <verify>
    <automated>npm test -- tests/preflight-release-candidate.test.ts tests/preflight.test.ts tests/result.test.ts tests/plugin.test.ts &amp;&amp; npm test &amp;&amp; npm run typecheck &amp;&amp; npm run build &amp;&amp; rm -f dist/release-candidate.js dist/release-candidate-result.js dist/release-candidate-node.js dist/release-candidate-remote-script.js dist/release-candidate-remote-executor.js dist/release-candidate-remote.js &amp;&amp; git diff --check &amp;&amp; test -r /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test ! -w /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test -r /tmp/dota-workshop-phase5-start.head &amp;&amp; test ! -w /tmp/dota-workshop-phase5-start.head &amp;&amp; PHASE5_START_HEAD=\"$(cat /tmp/dota-workshop-phase5-start.head)\" &amp;&amp; test -n \"$PHASE5_START_HEAD\" &amp;&amp; git cat-file -e \"${PHASE5_START_HEAD}^{commit}\" &amp;&amp; git merge-base --is-ancestor \"$PHASE5_START_HEAD\" HEAD &amp;&amp; (cd .planning/graphs &amp;&amp; shasum -a 256 GRAPH_REPORT.md graph.html graph.json &gt; /tmp/dota-workshop-phase5-graphs.current.sha256) &amp;&amp; cmp -s /tmp/dota-workshop-phase5-graphs.sha256 /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; rm /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; git diff --cached --quiet -- .planning/graphs</automated>
  </verify>
  <done>The source server advertises and routes one additive operation across all target contracts, structuredContent is exact, the old dry-run behavior is unchanged, and no undeclared generated runtime output remains.</done>
</task>

<task type="auto">
  <name>Task 3: Independently review public compatibility and routing</name>
  <files>src/tools.ts, src/server.ts, tests/preflight-release-candidate.test.ts, tests/preflight.test.ts, README.md, skills/dota2-workshop-tools/SKILL.md</files>
  <action>Independently compare the pre-change characterized `dry_run_release_report` objects and code path, inspect every target branch for correct service selection and zero fallback, validate the exact public schema has no prohibited fields, compare text content to structuredContent, and verify source/tool-list drift. Reproduce every confirmed issue in an atomic RED test before correction and rerun the complete gate. Run the graph comparison and cached-diff guard before each review commit. Record `requirements-completed: [RCOP-01]` only in `05-01-SUMMARY.md`.</action>
  <verify>
    <automated>npm test -- tests/preflight-release-candidate.test.ts tests/preflight.test.ts tests/plugin.test.ts &amp;&amp; npm test &amp;&amp; npm run typecheck &amp;&amp; git diff --check &amp;&amp; test -r /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test ! -w /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test -r /tmp/dota-workshop-phase5-start.head &amp;&amp; test ! -w /tmp/dota-workshop-phase5-start.head &amp;&amp; PHASE5_START_HEAD=\"$(cat /tmp/dota-workshop-phase5-start.head)\" &amp;&amp; test -n \"$PHASE5_START_HEAD\" &amp;&amp; git cat-file -e \"${PHASE5_START_HEAD}^{commit}\" &amp;&amp; git merge-base --is-ancestor \"$PHASE5_START_HEAD\" HEAD &amp;&amp; (cd .planning/graphs &amp;&amp; shasum -a 256 GRAPH_REPORT.md graph.html graph.json &gt; /tmp/dota-workshop-phase5-graphs.current.sha256) &amp;&amp; cmp -s /tmp/dota-workshop-phase5-graphs.sha256 /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; rm /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; git diff --cached --quiet -- .planning/graphs</automated>
  </verify>
  <done>No confirmed registration, dispatch, compatibility, runtime-build, or discoverability issue remains and RCOP-01 has one owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| MCP unknown input to target dispatcher | Unvalidated fields or wrong routing could introduce credentials, mutation controls, or fallback. |
| TypeScript public route to later runtime packaging | Import drift could leave 05-07 unable to produce a loadable packaged server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-01-01 | Spoofing | public operation and target branch | mitigate | Exact schema parsing and explicit discriminated target routing. |
| T-05-01-02 | Tampering | existing dry-run route | mitigate | Whole-result characterization and zero candidate-creation assertions. |
| T-05-01-03 | Repudiation | text versus structured MCP result | mitigate | Exact serialized equality and isError derived from normalized success. |
| T-05-01-04 | Tampering | source integration boundary | mitigate | Exact imports, source-level invocation tests, and a dependent packaging plan. |
</threat_model>

<prohibited_scope>
Do not change dry-run semantics, add another public operation or alias, accept credentials or lifecycle controls, bypass the strict local/remote services, add fallback/retry/repair, expose candidate roots, mutate sources or Workshop state, create archives, sign, encrypt, upload, compile, launch, or claim real Windows proof.
</prohibited_scope>

<verification>
Require RED before GREEN, focused MCP/dry-run/result/plugin tests, complete source tests/type/build checks, and patch hygiene. The out-of-repository graph baseline comparison plus cached-diff guard must pass before every commit. Remove generated candidate/Phase 5 runtime files after this plan; 05-07 alone decides and verifies the minimal tracked executable closure.
</verification>

<success_criteria>
- RCOP-01 appears only in this plan and its summary.
- One discoverable operation routes fixture, local, SSH, and PowerShell targets through their strict services.
- Existing dry-run behavior remains exact and inspection-only.
- MCP structuredContent, tool lists, and tracked runtime build match source behavior.
</success_criteria>

<output>
Create `.planning/phases/05-unified-mcp-and-remote-target-parity/05-01-SUMMARY.md` with `requirements-completed: [RCOP-01]` when done.
</output>
