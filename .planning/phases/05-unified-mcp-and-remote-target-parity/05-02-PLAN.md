---
phase: 05-unified-mcp-and-remote-target-parity
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/release-candidate-result.ts
  - src/types.ts
  - src/result.ts
  - tests/release-candidate-result.test.ts
  - tests/result.test.ts
autonomous: true
requirements:
  - RCOP-03
must_haves:
  truths:
    - "Users receive one immutable schema-versioned release-candidate detail in the common MCP envelope on both success and failure."
    - "Manifest, ledger, coverage, blocker, operation, cleanup, safe-path, execution, warning, command, log, and boundary domains remain machine-readable rather than being flattened into prose."
    - "Top-level success is recomputed from normalized operation, artifact, manifest, ledger, blocker, cleanup, and boundary invariants rather than trusting supplied success narration."
    - "Malformed versions, paths, digests, counts, ordering, state combinations, getters, proxies, and mutable aliases fail closed with stable sanitized evidence."
  artifacts:
    - path: "src/release-candidate-result.ts"
      provides: "Strict public detail types, canonical digest helper, hostile-input normalizer, deep-freeze boundary, and ToolResult projection"
      exports: ["normalizeReleaseCandidateDetail", "createReleaseCandidateToolResult", "computeReleaseCandidateCombinedDigest"]
    - path: "src/types.ts"
      provides: "Optional operation-specific releaseCandidate detail on the common ToolResult contract"
    - path: "tests/release-candidate-result.test.ts"
      provides: "Version, invariant, hostile-shape, immutable-result, and common-envelope matrices"
  key_links:
    - from: "Phase 4 lifecycle domains"
      to: "release-candidate detail"
      via: "strict projection without weakening operation, artifact, manifest, ledger, coverage, or cleanup semantics"
      pattern: "normalizeReleaseCandidateDetail"
    - from: "release-candidate detail"
      to: "ToolResult"
      via: "result builders preserve the detail only when explicitly supplied"
      pattern: "releaseCandidate"
    - from: "manifest entries"
      to: "combined digest and success"
      via: "ordinal fixed nested-array SHA-256 recomputation"
      pattern: "computeReleaseCandidateCombinedDigest"
---

<objective>
Define the single strict machine-readable detail and common-envelope authority for release-candidate preflight.

Purpose: Close RCOP-03 while preserving the already verified Phase 4 lifecycle domains and exact existing ToolResult shapes for unrelated operations.
Output: Public readonly detail types, a hostile-input normalizer, canonical digest recomputation, immutable projection, and common result integration.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-unified-mcp-and-remote-target-parity/05-CONTEXT.md
@.planning/phases/05-unified-mcp-and-remote-target-parity/05-RESEARCH.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-VERIFICATION.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-02-SUMMARY.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-03-SUMMARY.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-04-SUMMARY.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-06-SUMMARY.md
@src/release-candidate.ts
@src/types.ts
@src/result.ts
@tests/result.test.ts
</context>

<execution_guard>
The root orchestrator must have created the read-only Phase 5 preflight files `/tmp/dota-workshop-phase5-graphs.sha256` and `/tmp/dota-workshop-phase5-start.head` once before Wave 1. This plan and executor must never create, replace, refresh, or chmod them. Before any work and every commit, require both files, validate that the marker names an existing commit that is an ancestor of `HEAD`, compare current graph hashes to the immutable baseline, and require `git diff --cached --quiet -- .planning/graphs`; fail closed on any missing file, invalid marker, mismatch, or staged graph change.
</execution_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit the failing public-detail invariant matrix</name>
  <files>tests/release-candidate-result.test.ts, tests/result.test.ts</files>
  <behavior>
    - Version `1.0` success and blocked details preserve operation, artifact validation, manifest, exact inclusion ledger, exhaustive scan coverage, deterministic blockers, cleanup, safe paths, execution evidence, warnings, and boundary facts.
    - Canonical digest recomputation uses the Phase 4 fixed nested-array encoding and rejects altered, unordered, duplicated, unsafe-path, invalid-count, or invalid-digest entries.
    - Artifact passed requires a manifest, bijective counts, complete non-overlapping coverage, no blockers, completed operation, and verified one-attempt cleanup; contradictory or incomplete states cannot produce top-level success.
    - Every returned domain is deeply frozen and independently cloned; mutation of adapter-owned arrays or blocker objects after normalization cannot change the result.
    - Getter, Proxy, throwing array access, unsupported version, unknown discriminant/code, unsafe absolute or traversal path, uppercase digest, fractional count, and hostile thenable cases produce a stable sanitized normalization failure with no unproven nested facts.
    - Existing success and failure result builders remain byte-equivalent when no release-candidate detail is supplied.
    - Equivalent fixture/local and remote complete facts normalize to the same substantive detail, while transport-uncertain facts remain observably different.
  </behavior>
  <action>Write behavior-first tests for the locked public structured-evidence and invariant decisions. Build valid success, valid artifact blocker, cleanup-only failure, and precreation failure fixtures directly from Phase 4 domain shapes. Add independent canonical vectors, hostile unknown-input cases, and responsibility-specific parity cases proving equivalent Node/remote complete facts normalize identically while uncertainty stays distinct. Assert the optional common-envelope field is absent from every existing result fixture unless explicitly supplied. Run the focused tests; they must fail because the public detail module and common result field do not exist. Stop on unexpected GREEN. Compare graph hashes and require the cached graph diff to be empty immediately before committing only tests as `test(05-02): require strict candidate detail`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate-result.test.ts tests/result.test.ts &amp;&amp; test -r /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test ! -w /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test -r /tmp/dota-workshop-phase5-start.head &amp;&amp; test ! -w /tmp/dota-workshop-phase5-start.head &amp;&amp; PHASE5_START_HEAD=\"$(cat /tmp/dota-workshop-phase5-start.head)\" &amp;&amp; test -n \"$PHASE5_START_HEAD\" &amp;&amp; git cat-file -e \"${PHASE5_START_HEAD}^{commit}\" &amp;&amp; git merge-base --is-ancestor \"$PHASE5_START_HEAD\" HEAD &amp;&amp; (cd .planning/graphs &amp;&amp; shasum -a 256 GRAPH_REPORT.md graph.html graph.json &gt; /tmp/dota-workshop-phase5-graphs.current.sha256) &amp;&amp; cmp -s /tmp/dota-workshop-phase5-graphs.sha256 /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; rm /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; git diff --cached --quiet -- .planning/graphs</automated>
  </verify>
  <done>An atomic RED commit proves the missing versioned detail, invariant recomputation, hostile normalization, immutability, and optional-envelope behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize and project the versioned detail</name>
  <files>src/release-candidate-result.ts, src/types.ts, src/result.ts</files>
  <behavior>
    - One strict normalizer accepts unknown adapter or transport facts and returns either a deeply frozen valid detail or a closed sanitized failure.
    - Canonical manifest, ledger, coverage, blocker, operation, and cleanup invariants are recomputed rather than trusted.
    - ToolResult exposes the detail through one optional readonly field without changing unrelated operation results.
  </behavior>
  <action>Implement explicit closed TypeScript types and guarded normalization for the release-candidate detail. Reuse Phase 4 manifest, ledger, coverage, operation, artifact, blocker, and cleanup contracts; export the exact canonical digest helper instead of transcribing a second algorithm. Snapshot recognized foreign fields by guarded indexed access, validate exact versions and discriminants, ordinal safe paths, lowercase SHA-256, safe counts, ledger/coverage consistency, blocker/state consistency, and cleanup proof. Define the fixed boundary shape in the contract but leave exact mandatory boundary values and input-field prohibition to 05-03. Add `releaseCandidate?: ReleaseCandidateDetail` to ToolResult and pass it through result builders only when supplied so existing object equality remains unchanged. Compute top-level `ok` from normalized facts; never accept payload narration as authority. Make the RED suite GREEN, remove generated untracked Phase 5 distribution files after build, compare graph hashes, require the cached graph diff to be empty, and commit production code as `feat(05-02): normalize candidate detail`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate-result.test.ts tests/result.test.ts &amp;&amp; npm test &amp;&amp; npm run typecheck &amp;&amp; npm run build &amp;&amp; rm -f dist/release-candidate.js dist/release-candidate-result.js dist/release-candidate-node.js dist/release-candidate-remote-script.js dist/release-candidate-remote-executor.js dist/release-candidate-remote.js &amp;&amp; npm run verify:plugin &amp;&amp; npm run verify:same-machine-smoke &amp;&amp; npm run verify:source-snapshot &amp;&amp; npm run verify:install-simulation &amp;&amp; npm run verify:rc &amp;&amp; npm run verify:handoff &amp;&amp; npm run verify:milestone &amp;&amp; git diff --check &amp;&amp; test -r /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test ! -w /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test -r /tmp/dota-workshop-phase5-start.head &amp;&amp; test ! -w /tmp/dota-workshop-phase5-start.head &amp;&amp; PHASE5_START_HEAD=\"$(cat /tmp/dota-workshop-phase5-start.head)\" &amp;&amp; test -n \"$PHASE5_START_HEAD\" &amp;&amp; git cat-file -e \"${PHASE5_START_HEAD}^{commit}\" &amp;&amp; git merge-base --is-ancestor \"$PHASE5_START_HEAD\" HEAD &amp;&amp; (cd .planning/graphs &amp;&amp; shasum -a 256 GRAPH_REPORT.md graph.html graph.json &gt; /tmp/dota-workshop-phase5-graphs.current.sha256) &amp;&amp; cmp -s /tmp/dota-workshop-phase5-graphs.sha256 /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; rm /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; git diff --cached --quiet -- .planning/graphs</automated>
  </verify>
  <done>The common result can carry one strict immutable detail, existing results retain their exact shape, and all focused/full gates pass without retaining generated untracked distribution files.</done>
</task>

<task type="auto">
  <name>Task 3: Independently review the public invariant boundary</name>
  <files>src/release-candidate-result.ts, src/types.ts, src/result.ts, tests/release-candidate-result.test.ts, tests/result.test.ts</files>
  <action>Run an implementation-independent review of strict version handling, foreign collection access, digest recomputation, occurrence counting before maps, coverage completeness, cleanup precedence, blocker determinism, deep freeze/clone separation, callback-value/path exclusion, responsibility-specific parity, and compatibility of unrelated ToolResult objects. Reproduce every confirmed defect with a new failing test before an isolated corrective commit. Run the graph comparison and cached-diff guard before each review commit. Record `requirements-completed: [RCOP-03]` and the RED/GREEN commit sequence in `05-02-SUMMARY.md`; no other Phase 5 requirement may appear in that field.</action>
  <verify>
    <automated>npm test -- tests/release-candidate-result.test.ts tests/result.test.ts &amp;&amp; npm test &amp;&amp; npm run typecheck &amp;&amp; git diff --check &amp;&amp; test -r /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test ! -w /tmp/dota-workshop-phase5-graphs.sha256 &amp;&amp; test -r /tmp/dota-workshop-phase5-start.head &amp;&amp; test ! -w /tmp/dota-workshop-phase5-start.head &amp;&amp; PHASE5_START_HEAD=\"$(cat /tmp/dota-workshop-phase5-start.head)\" &amp;&amp; test -n \"$PHASE5_START_HEAD\" &amp;&amp; git cat-file -e \"${PHASE5_START_HEAD}^{commit}\" &amp;&amp; git merge-base --is-ancestor \"$PHASE5_START_HEAD\" HEAD &amp;&amp; (cd .planning/graphs &amp;&amp; shasum -a 256 GRAPH_REPORT.md graph.html graph.json &gt; /tmp/dota-workshop-phase5-graphs.current.sha256) &amp;&amp; cmp -s /tmp/dota-workshop-phase5-graphs.sha256 /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; rm /tmp/dota-workshop-phase5-graphs.current.sha256 &amp;&amp; git diff --cached --quiet -- .planning/graphs</automated>
  </verify>
  <done>Independent review has no unresolved confirmed issue, RCOP-03 has exactly one summary owner, and graph files remain untouched and unstaged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Lifecycle or transport facts to public MCP detail | Foreign objects may spoof success, mutate after return, leak private identities, or contradict Phase 4 evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-02-01 | Spoofing | adapter-supplied success and cleanup facts | mitigate | Recompute all success invariants and accept only closed versioned facts. |
| T-05-02-02 | Tampering | manifest, ledger, coverage, and blocker collections | mitigate | Guarded occurrence snapshots, exact counts/order, independent clones, and deep freeze. |
| T-05-02-03 | Information Disclosure | paths and hostile parse errors | mitigate | Allow only safe relative identities and return stable category-only normalization failures. |
</threat_model>

<prohibited_scope>
Do not register the MCP operation, implement filesystem or remote execution, change `dry_run_release_report`, add credentials, expose a candidate root or capability, retry/repair malformed evidence, mutate sources, create persistent artifacts, invoke Steam/Workshop behavior, compile, sign, encrypt, upload, or claim real Windows proof.
</prohibited_scope>

<verification>
Require an atomic failing RED commit before GREEN. Run focused detail/result tests, the complete test suite, typecheck, build, every existing verification command, and patch hygiene. Remove generated untracked candidate/Phase 5 distribution files after builds. The fixed external graph baseline comparison and cached-diff guard must pass before every commit.
</verification>

<success_criteria>
- RCOP-03 appears in this plan and its summary only once across Phase 5.
- The public detail is explicit, versioned, immutable, semantically normalized, and carried through the common envelope on success and failure.
- Top-level success cannot be spoofed by exit code, adapter `ok`, narration, malformed nested facts, or cleanup contradiction.
- Existing operation result shapes and `dry_run_release_report` behavior remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/05-unified-mcp-and-remote-target-parity/05-02-SUMMARY.md` with `requirements-completed: [RCOP-03]` when done.
</output>
