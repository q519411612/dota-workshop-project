---
phase: 05-unified-mcp-and-remote-target-parity
plan: 07
subsystem: packaged-mcp-runtime
tags: [typescript, dist, git-index, import-closure, tdd]

requires:
  - phase: 05-unified-mcp-and-remote-target-parity
    plan: 01
    provides: Source-level preflight_release_candidate schema, registration, and dispatch route
provides:
  - Minimal tracked compiled import closure for the candidate-preflight MCP runtime
  - Byte-for-byte isolated-build versus staged-index verification
  - Plain Node load and invocation from a Git-index-only export
affects: [05-06, plugin-installation, packaged-runtime]

tech-stack:
  added: []
  patterns: [isolated compiler output, staged blob comparison, index-only runtime export]

key-files:
  created: [tests/packaged-release-candidate-runtime.test.ts, dist/release-candidate.js, dist/release-candidate-result.js, dist/release-candidate-node.js, dist/release-candidate-remote-script.js, dist/release-candidate-remote-executor.js, dist/release-candidate-remote.js]
  modified: [dist/result.js, dist/schemas.js, dist/tools.js, dist/server.js]

key-decisions:
  - "Only runtime files proven reachable from compiled server.js and different from tracked bytes are promoted."
  - "dist/types.js is type-only, unchanged, and absent from the runtime import closure, so it is not promoted as a false dependency."
  - "The index export is created in an isolated temporary directory under the repository so a plain Node process resolves installed package dependencies without using worktree dist bytes."

patterns-established:
  - "Every reachable compiled module is compared with git show :path before checkout-index export and invocation."

requirements-completed: []

duration: 7min
completed: 2026-07-16
status: complete
---

# Phase 5 Plan 07: Packaged Candidate Runtime Summary

**The tracked MCP runtime now contains the minimal executable release-candidate import closure and is proven byte-identical to an isolated build before loading and invoking the operation from a Git-index-only export.**

## Accomplishments

- Added a packaged-runtime test that compiles into a fresh temporary directory and derives local imports recursively from `server.js`.
- Compared every reachable isolated compiler output byte-for-byte with its staged Git blob.
- Promoted only the six new release-candidate modules plus the four changed existing runtime modules required by the reachable closure.
- Exported the repository solely from the Git index and invoked `preflight_release_candidate` through the exported runtime.
- Verified plugin packaging and install simulation with no undeclared `dist/*.js` output.
- Kept compiled MCP modules distinct from addon candidates, archives, upload payloads, signatures, encrypted data, credentials, or source transformations.

## TDD Evidence

- RED commit `c682df8` failed because the staged runtime lacked `dist/release-candidate-node.js` and the rest of the new reachable closure.
- GREEN commit `9019575` promoted the exact isolated compiler bytes and made the index-exported invocation pass.
- Review RED commit `6f6598b` proved Vitest module resolution could hide a plain-Node dependency-resolution failure.
- Review GREEN commit `74bcacc` moved the index export to an isolated workspace child and invoked it with a separate Node process.

## Independent Review

- Confirmed closure derivation starts from isolated `server.js` and follows only relative runtime `.js` imports.
- Confirmed every reachable module is read from the staged index, never accepted from worktree-generated bytes.
- Confirmed the plain Node subprocess loads only index-exported `dist/server.js` and `dist/tools.js`.
- Confirmed exactly one fixture service invocation and zero remote calls for the deterministic packaged route test.
- Confirmed `dist/types.js` is not a runtime import and remained unchanged rather than being promoted solely because it appeared in a planning file list.
- Confirmed no undeclared generated JavaScript or candidate/archive artifact remains.
- Final review result: no unresolved confirmed issue.

## Verification Evidence

- Final packaged/public/plugin suite: 16/16 passed.
- `npm run typecheck`: passed.
- `npm run verify:plugin`: passed.
- `npm run verify:install-simulation`: passed with cleanup evidence.
- No untracked `dist/*.js` files exist.
- `git diff --check`: passed.
- Immutable graph baseline and cached graph exclusion guards passed before every commit.

## Security and Scope Review

- No candidate directory, addon tree, archive, signature, encrypted output, credential, private target, upload payload, or generated source content was tracked.
- TypeScript compilation produced executable MCP runtime modules only; it did not compile or repair addon source files.
- No Steam login, Workshop mutation, upload, remote Windows execution, or real Windows proof occurred.
- User-owned `.planning/graphs/` modifications remain untouched, unstaged, and outside every commit.

## Self-Check: PASSED

- All declared executable closure and test artifacts exist.
- RED `c682df8` precedes GREEN `9019575`; review RED `6f6598b` precedes review GREEN `74bcacc`.
- `requirements-completed` is empty as required.
- The worktree contains only the preserved user-owned graph modifications.

---
*Phase: 05-unified-mcp-and-remote-target-parity*
*Completed: 2026-07-16*
