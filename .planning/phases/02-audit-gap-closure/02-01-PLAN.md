---
phase: 02-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/install-simulation.ts
  - tests/install-simulation.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/01-local-install-simulation/01-VERIFICATION.md
  - .planning/phases/01-local-install-simulation/01-REVIEW.md
  - .planning/phases/01-local-install-simulation/01-01-SUMMARY.md
  - .planning/phases/02-audit-gap-closure/02-01-SUMMARY.md
autonomous: true
requirements: [INSTALL-01, INSTALL-02, INSTALL-03, INSTALL-04, INSTALL-05, INSTALL-06]
must_haves:
  truths:
    - "Copied YAML/YML inputs are scanned case-insensitively before the simulation can report success."
    - "The real copied agents/openai.yaml path yields a redacted blocker, never exposes the matched value, and is cleaned up."
    - "A missing dist/index.js yields exactly one blocker per code/path pair."
    - "Phase 1 requirements, verification, review, and summary artifacts accurately reflect INSTALL-01 through INSTALL-06 after re-verification."
    - "STATE and ROADMAP route v1.13 Phase 2 without changing historical milestone sections or graph files."
  artifacts:
    - path: "src/install-simulation.ts"
      provides: "Case-insensitive YAML/YML recognition and stable blocker uniqueness"
    - path: "tests/install-simulation.test.ts"
      provides: "RED/GREEN regression coverage for YAML/YML, redaction, cleanup, and blocker uniqueness"
      contains: "agents/openai.yaml"
    - path: ".planning/REQUIREMENTS.md"
      provides: "A dedicated Traceability table for all six INSTALL requirements"
      contains: "## Traceability"
    - path: ".planning/phases/01-local-install-simulation/01-REVIEW.md"
      provides: "Superseding re-review evidence that removes the stale clean verdict"
    - path: ".planning/phases/01-local-install-simulation/01-01-SUMMARY.md"
      provides: "Exact requirements-completed metadata"
  key_links:
    - from: "tests/install-simulation.test.ts"
      to: "src/install-simulation.ts"
      via: "simulateLocalInstall behavior on copied skill metadata fixtures"
      pattern: "simulateLocalInstall"
    - from: ".planning/phases/01-local-install-simulation/01-REVIEW.md"
      to: ".planning/phases/02-audit-gap-closure/02-CONTEXT.md"
      via: "Re-review reference to the Phase 2 closure and corrected YAML finding"
      pattern: "Phase 2"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "Matching v1.13 and Phase 2 routing"
      pattern: "current_phase: 2"
---

<objective>
Repair the functional install-simulation gap and make Phase 1 traceability, review, and state evidence accurate.

Purpose: Close the root cause with a strict RED→GREEN cycle, then remove the stale evidence that allowed the gap to appear satisfied.
Output: Tested source changes, exact Phase 1 requirement metadata, a superseding Phase 1 re-review, and synchronized v1.13 routing.
</objective>

<execution_context>
@/Users/chenminghui/.codex/gsd-core/workflows/execute-plan.md
@/Users/chenminghui/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.13-MILESTONE-AUDIT.md
@.planning/phases/02-audit-gap-closure/02-SPEC.md
@.planning/phases/02-audit-gap-closure/02-CONTEXT.md
@.planning/phases/01-local-install-simulation/01-VERIFICATION.md
@.planning/phases/01-local-install-simulation/01-REVIEW.md
@.planning/phases/01-local-install-simulation/01-01-SUMMARY.md
@src/install-simulation.ts
@tests/install-simulation.test.ts

The graph is stale and returned no phase-relevant nodes. Existing `.planning/graphs/` changes are user-owned and excluded per D-06.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close YAML/YML scanning and duplicate blocker gaps through RED→GREEN</name>
  <files>tests/install-simulation.test.ts, src/install-simulation.ts</files>
  <behavior>
    - A synthetic credential at `skills/dota2-workshop-tools/agents/openai.yaml` returns `ok: false`, exactly one category/path-only sensitive blocker, excludes the matched value from serialized output, and reports cleanup removal.
    - `.yml`, `.YAML`, and `.YML` inputs receive the same scanning behavior.
    - Omitting `dist/index.js` returns exactly one `SIM_DIST_INDEX_MISSING` blocker for `dist/index.js` and still removes the simulation root.
    - Existing success, isolation, contract, environment, and Markdown-sensitive tests remain green.
  </behavior>
  <action>Extend the fixture to create the real copied metadata path and YAML/YML variants. Add the behavior assertions required by D-02 and D-03, run the targeted suite before production edits, and record RED failures caused by extension skipping and duplicate missing-dist evidence. Stop if either new assertion is unexpectedly green. Then make only the root-cause changes: normalize the extracted extension and explicitly recognize `.yaml` and `.yml` per D-01; enforce stable uniqueness by blocker code/path pair per D-03. Preserve the result schema, explicit text allowlist, category/path-only evidence, and cleanup in `finally`. Do not add content heuristics or deferred external behavior. Do not touch `.planning/graphs/` per D-06.</action>
  <verify>
    <automated>npm test -- tests/install-simulation.test.ts</automated>
  </verify>
  <done>The new tests first fail for the diagnosed causes, then pass after the smallest production change with exact blocker counts, redaction, cleanup, and existing behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Repair Phase 1 traceability, supersede stale review evidence, and synchronize routing</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/01-local-install-simulation/01-VERIFICATION.md, .planning/phases/01-local-install-simulation/01-REVIEW.md, .planning/phases/01-local-install-simulation/01-01-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/02-audit-gap-closure/02-01-SUMMARY.md</files>
  <action>After functional GREEN, add a dedicated `## Traceability` table to REQUIREMENTS and Phase 1 VERIFICATION. Each table must contain exactly one row for each INSTALL-01 through INSTALL-06 and cite concrete implementation, test, command, and artifact evidence per D-04. Convert Phase 1 SUMMARY to valid GSD frontmatter with `requirements-completed` equal to the exact ordered six-ID set. Update Phase 1 REVIEW so the old “No blocking issues found” conclusion cannot remain the active verdict: explicitly record the audit-discovered YAML miss, the corrected implementation and regression evidence, the independent re-review result, and a reference to Phase 2 closure. Retain historical context only when clearly labeled as superseded. Update only current v1.13 Phase 2 fields in ROADMAP and STATE per D-05; preserve historical milestone sections. Create the Plan 01 summary with exact six-ID metadata and actual RED/GREEN evidence. Do not alter `.planning/graphs/` per D-06.</action>
  <verify>
    <automated>node -e 'const fs=require("fs"),cp=require("child_process"); const ids=["INSTALL-01","INSTALL-02","INSTALL-03","INSTALL-04","INSTALL-05","INSTALL-06"]; const table=(p)=&gt;{const s=fs.readFileSync(p,"utf8"); const m=s.match(/## Traceability\s*\n([\s\S]*?)(?=\n## |$)/); if(!m) process.exit(1); const rows=m[1].split("\n").filter(x=&gt;/^\|\s*INSTALL-\d{2}\s*\|/.test(x)).map(x=&gt;x.split("|")[1].trim()); if(JSON.stringify(rows)!==JSON.stringify(ids)) process.exit(1)}; table(".planning/REQUIREMENTS.md"); table(".planning/phases/01-local-install-simulation/01-VERIFICATION.md"); const exactSummary=(p)=&gt;{const out=cp.execFileSync("node",["/Users/chenminghui/.codex/gsd-core/bin/gsd-tools.cjs","query","summary-extract",p,"--fields","requirements_completed","--pick","requirements_completed"],{encoding:"utf8"}).trim(); const actual=out?out.split(","):[]; if(JSON.stringify(actual)!==JSON.stringify(ids)) process.exit(1)}; exactSummary(".planning/phases/01-local-install-simulation/01-01-SUMMARY.md"); exactSummary(".planning/phases/02-audit-gap-closure/02-01-SUMMARY.md"); const state=fs.readFileSync(".planning/STATE.md","utf8").match(/^---\n([\s\S]*?)\n---/); if(!state||!/^milestone:\s*v1\.13$/m.test(state[1])||!/^current_phase:\s*2$/m.test(state[1])) process.exit(1); const init=JSON.parse(cp.execFileSync("node",["/Users/chenminghui/.codex/gsd-core/bin/gsd-tools.cjs","query","init.phase-op","2"],{encoding:"utf8"})); if(init.phase_found!==true||init.padded_phase!=="02"||init.phase_dir!==".planning/phases/02-audit-gap-closure"||init.phase_name!=="audit-gap-closure") process.exit(1); const review=fs.readFileSync(".planning/phases/01-local-install-simulation/01-REVIEW.md","utf8"); if(!/supersed/i.test(review)||!/Phase 2/.test(review)||!/YAML/i.test(review)) process.exit(1)'</automated>
  </verify>
  <done>Both Traceability tables contain the exact six rows, both Phase 1 and Plan 01 summaries extract the exact six-ID set, Phase 1 REVIEW machine-checkably supersedes its stale verdict, STATE and `init.phase-op 2` resolve the exact v1.13 Phase 2 identity, and graph files remain untouched.</done>
</task>

</tasks>

<source_coverage>
| Source | ID | Requirement or decision | Task | Status |
|---|---|---|---|---|
| GOAL | — | Close sensitive scanning and traceability gaps | 1, 2 | COVERED |
| REQ | INSTALL-01 | Preserve structured simulation command | 1, 2 | COVERED |
| REQ | INSTALL-02 | Preserve isolated layout consumption | 1 | COVERED |
| REQ | INSTALL-03 | Preserve consumer checks and unique missing-entry evidence | 1, 2 | COVERED |
| REQ | INSTALL-04 | Preserve cleanup and environment safety | 1, 2 | COVERED |
| REQ | INSTALL-05 | Scan YAML/YML without value leakage | 1, 2 | COVERED |
| REQ | INSTALL-06 | Repair tests, traceability, verification, and review evidence | 1, 2 | COVERED |
| RESEARCH | — | Skipped; established implementation patterns are sufficient | 1 | COVERED |
| CONTEXT | D-01 | Explicit normalized YAML/YML allowlist | 1 | COVERED |
| CONTEXT | D-02 | Real metadata path and value-free blocker | 1 | COVERED |
| CONTEXT | D-03 | Unique blocker and cleanup | 1 | COVERED |
| CONTEXT | D-04 | Exact three-source requirement mapping | 2 | COVERED |
| CONTEXT | D-05 | Exact v1.13 Phase 2 routing | 2 | COVERED |
| CONTEXT | D-06 | Preserve graphs | 1, 2 | COVERED |
</source_coverage>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Repository inputs → simulation layout | Recursively copied plugin files are untrusted scanning inputs. |
| Sensitive content → serialized evidence | Secret values must never cross into results or planning artifacts. |
| Generated root → host filesystem | Cleanup must remain limited to the temporary simulation root. |
| Historical evidence → current verdict | Stale review claims must be explicitly superseded by current evidence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Information Disclosure | Sensitive scan results | mitigate | Assert category/path only and serialized-value exclusion per D-02. |
| T-02-02 | Tampering | Extension recognition | mitigate | Normalize and explicitly allow YAML/YML; test case variants per D-01. |
| T-02-03 | Repudiation | Traceability and review artifacts | mitigate | Exact row-set assertions and superseding re-review evidence per D-04. |
| T-02-04 | Denial of Service | Duplicate blockers | mitigate | Stable uniqueness and exact-count assertion per D-03. |
| T-02-05 | Elevation of Privilege | Local simulation boundary | accept | No privilege or external-operation change is introduced. |
| T-02-06 | Tampering | Graph artifacts | mitigate | Exclude and preserve all graph changes per D-06. |
| T-02-SC | Tampering | Dependencies | accept | No dependency installation is planned. |
</threat_model>

<verification>
- Observe targeted RED failures before production edits, then targeted GREEN.
- Exact table and summary assertions must pass without accepting subsets or extra INSTALL IDs.
- STATE frontmatter and `init.phase-op 2` must match exact v1.13 Phase 2 identity.
- Phase 1 REVIEW must explicitly supersede its stale verdict.
</verification>

<success_criteria>
- YAML/YML cannot bypass scanning and secret values remain absent from evidence.
- Missing dist evidence is unique and cleanup remains deterministic.
- Phase 1 traceability and review artifacts accurately represent the corrected state.
- v1.13 Phase 2 routing is exact and graph changes are preserved.
</success_criteria>

<output>
Create `.planning/phases/02-audit-gap-closure/02-01-SUMMARY.md` when done.
</output>
