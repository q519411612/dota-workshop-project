# Phase 5 Plan: Runtime Marker Validation

## Scope

Implement explicit runtime validation support without changing the v1 Workshop Tools editor path.

## Tasks

1. Add `runtimeMode` and `consoleLog` inputs to `launch_custom_game`.
2. Build local and remote launch arguments from shared intent:
   - `runtimeMode: "tools"` keeps `-tools`.
   - `runtimeMode: "game"` omits `-tools`.
   - `consoleLog: true` adds `-console -condebug`.
3. Preserve existing remote `launchMode: "interactiveTask"` behavior.
4. Update marker validation to match substrings inside log lines.
5. Ensure remote log discovery can surface `game/dota/console.log`.
6. Add fixture tests for command construction and prefixed marker validation.
7. Run real remote smoke with a generated addon, runtime mode, and console logging.
8. Update README, skill references, STATE, and smoke evidence.
9. Run verification, independent review, commit, and push.

## Verification

- `npm test`
- `npm run typecheck`
- `npm run build`
- Plugin manifest validation
- Skill validation
- Secret scan for credentials and private target data
- Real remote smoke evidence for `[VScript] [DOTA_WORKSHOP_MCP]` in `game/dota/console.log`
