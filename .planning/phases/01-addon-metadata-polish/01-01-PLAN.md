# Plan: Addon Metadata Polish

**Status:** Ready
**Date:** 2026-07-07

## Implementation Approach

1. Add failing tests for generated addon metadata and dry-run metadata blockers/evidence.
2. Extend `addoninfo.txt` generation with release review metadata.
3. Extend dry-run release metadata checks to cover title, author, description, version, default map, and maps entry.
4. Run targeted tests, build, and repository gates.
5. Record verification, independent review, and summary artifacts.

## Planned Files

- `tests/addon.test.ts`
- `tests/preflight.test.ts`
- `src/addon.ts`
- `src/preflight.ts`
- `.planning/phases/01-addon-metadata-polish/01-VERIFICATION.md`
- `.planning/phases/01-addon-metadata-polish/01-REVIEW.md`
- `.planning/phases/01-addon-metadata-polish/01-01-SUMMARY.md`

## Verification Commands

- `npm test -- tests/addon.test.ts tests/preflight.test.ts`
- `npm run build`
- `git diff --check`
- `npm run typecheck`
- `npm test`
- `npm run verify:rc`
- `npm run verify:handoff`
- `npm run verify:milestone`

## Risks

- Existing release tests depend on metadata blocker counts, so the tests must be updated with the new required fields.
- The slice must remain dry-run only and must not introduce publishing behavior.
