---
wave: 1
depends_on: []
requirements_addressed: [HAND-01, HAND-02, HAND-03, HAND-04, HAND-05, HAND-06]
autonomous: true
---

# Phase 1 Plan: Plugin Readiness Verifier

## Objective

Add a local plugin handoff readiness verifier that catches manifest, entrypoint, skill reference, and documented tool-list drift before installation or operator handoff.

## Tasks

<task id="01-01-T1" type="tdd">
<title>Add verifier drift tests</title>
<read_first>
- `.planning/REQUIREMENTS.md`
- `.planning/phases/01-plugin-install-handoff-readiness/01-SPEC.md`
- `package.json`
- `.codex-plugin/plugin.json`
- `.mcp.json`
- `src/tools.ts`
- `README.md`
- `skills/dota2-workshop-tools/SKILL.md`
</read_first>
<action>
Create `tests/plugin.test.ts` with fixture-based tests for plugin readiness verification. Cover a passing fixture, missing manifest blocker, bad MCP entrypoint blocker, missing skill reference blocker, extra documented tool blocker, and missing documented tool blocker.
</action>
<acceptance_criteria>
- `npx vitest run tests/plugin.test.ts` fails before implementation because the verifier module does not exist.
- Test names identify the blocker category they prove.
</acceptance_criteria>
</task>

<task id="01-01-T2" type="implementation">
<title>Implement local plugin readiness verifier</title>
<read_first>
- `src/tools.ts`
- `src/types.ts`
- `src/result.ts`
- `tests/plugin.test.ts`
</read_first>
<action>
Create `src/plugin.ts` with `verifyPluginPackage(input)` and `src/verify-plugin.ts` CLI. The verifier reads repository files, checks manifest/config/package entrypoints, verifies skill references, compares README and skill tool lists against `toolNames`, returns structured evidence/blockers, and never accepts credentials or remote target inputs.
</action>
<acceptance_criteria>
- `npx vitest run tests/plugin.test.ts` passes.
- The verifier reports blockers with stable codes for missing files, invalid JSON, bad entrypoints, missing skill references, and tool-list drift.
- CLI exits zero when `ok` is true and non-zero when blockers exist.
</acceptance_criteria>
</task>

<task id="01-01-T3" type="documentation">
<title>Wire handoff command and documentation</title>
<read_first>
- `package.json`
- `README.md`
- `skills/dota2-workshop-tools/SKILL.md`
- `src/tools.ts`
</read_first>
<action>
Add `verify:plugin` to `package.json`, update README with a plugin handoff readiness section, and correct the skill MCP tool contract so its documented operations exactly match `toolNames`.
</action>
<acceptance_criteria>
- `package.json` contains `verify:plugin`.
- README contains `npm run build`, `npm run verify:plugin`, and a no-credentials boundary in the handoff section.
- Skill tool contract no longer lists `link_addon` and does list `remote_command`.
</acceptance_criteria>
</task>

<task id="01-01-T4" type="verification">
<title>Verify and close v1.4</title>
<read_first>
- `.planning/phases/01-plugin-install-handoff-readiness/01-SPEC.md`
- `.planning/phases/01-plugin-install-handoff-readiness/01-01-PLAN.md`
- `package.json`
</read_first>
<action>
Run targeted and full verification, strict secret scan, write `01-VERIFICATION.md`, `01-REVIEW.md`, and `01-01-SUMMARY.md`, update GSD state to complete, then commit and push only v1.4 changes while leaving graphify freshness files uncommitted.
</action>
<acceptance_criteria>
- `npm run build` exits 0 before `npm run verify:plugin`.
- `npm run verify:plugin` exits 0.
- `git diff --check`, `npm run typecheck`, `npm test`, and strict secret scan pass.
- Independent review records no blocking issues or lists fixes before commit.
- Graphify freshness files remain uncommitted.
</acceptance_criteria>
</task>

## Artifacts This Phase Produces

- `src/plugin.ts`
- `src/verify-plugin.ts`
- `dist/plugin.js`
- `dist/verify-plugin.js`
- `tests/plugin.test.ts`
- `package.json` script `verify:plugin`
- README handoff readiness section
- Updated skill MCP tool contract
- `.planning/phases/01-plugin-install-handoff-readiness/01-VERIFICATION.md`
- `.planning/phases/01-plugin-install-handoff-readiness/01-REVIEW.md`
- `.planning/phases/01-plugin-install-handoff-readiness/01-01-SUMMARY.md`

## Must Haves

truths:
- `npm run verify:plugin` must exist and run locally.
- The verifier must compare README and skill documented tools against `toolNames`.
- The verifier must block missing files, invalid JSON, missing references, bad entrypoints, and tool-list drift.
- README must document the handoff command and no-credentials boundary.

prohibitions:
- statement: Must not store credentials, tokens, private keys, private hosts, or passwords.
  status: resolved
  verification: test
- statement: Must not perform global plugin installation.
  status: resolved
  verification: judgment
- statement: Must not publish packages or create upload artifacts.
  status: resolved
  verification: judgment
- statement: Must not perform Steam login, encryption, Workshop upload, or publish-state mutation.
  status: resolved
  verification: judgment
- statement: Must not silently ignore missing files, malformed JSON, or tool-list drift.
  status: resolved
  verification: test

## Verification Commands

- `npx vitest run tests/plugin.test.ts`
- `npm run build`
- `npm run verify:plugin`
- `git diff --check`
- `npm run typecheck`
- `npm test`
- Strict high-signal secret scan
