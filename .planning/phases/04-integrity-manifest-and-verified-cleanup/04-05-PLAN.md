---
phase: 04-integrity-manifest-and-verified-cleanup
plan: 05
type: tdd
wave: 5
depends_on: ["04-04"]
files_modified:
  - tests/release-candidate.test.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCCL-01
must_haves:
  truths:
    - "Users receive cleanup evidence after every outcome once candidate creation has occurred."
    - "Every valid acquired lease reaches exactly one cleanup attempt for success, validation blockers, copy/read/hash failures, callback failure, and malformed adapter results."
    - "A malformed or exceptional acquisition after target creation cannot be mislabeled as a precreation failure or silently orphan state."
  artifacts:
    - path: "src/release-candidate.ts"
      provides: "Strict acquisition boundary and one post-create cleanup funnel"
    - path: "tests/release-candidate.test.ts"
      provides: "Post-create fault matrix with exact creation and cleanup call counts"
  key_links:
    - from: "candidate creation/acquisition outcome"
      to: "cleanup ownership"
      via: "versioned acquired-or-created-failure contract"
      pattern: "createCandidateLease"
    - from: "every acquired lease outcome"
      to: "cleanup evidence"
      via: "one unconditional finally call"
      pattern: "finally|cleanupCandidateLease"
---

<objective>
Make cleanup ownership explicit at the instant a temporary candidate can exist and attempt cleanup exactly once thereafter.

Purpose: Close RCCL-01 across all post-create success, blocker, exception, and malformed-result paths.
Output: Strict acquisition semantics, one cleanup funnel, exact attempt evidence, and exhaustive fault-injection tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-CONTEXT.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-RESEARCH.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-PATTERNS.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-04-SUMMARY.md
@src/release-candidate.ts
@tests/release-candidate.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify exactly-once cleanup across post-create faults</name>
  <files>tests/release-candidate.test.ts</files>
  <behavior>
    - Success, readiness/structure/ledger/hash/scan blockers, copy/read/hash exceptions, callback throw, and hostile observation results each report one cleanup attempt after one creation.
    - Cleanup is neither skipped nor retried when validation or cleanup parsing fails.
    - Simulated creation followed by malformed, getter/Proxy/thenable, or exceptional lease acquisition returns post-create cleanup evidence and leaves no fixture directory.
    - True precreation validation/inventory failure still reports zero creation and zero cleanup attempts.
  </behavior>
  <action>Add a table-driven fixture fault matrix with creation count, cleanup count, callback count, and post-test filesystem absence assertions. Include malformed/throwing acquisition results after the fixture records target creation, plus malformed copy/read/hash/reconciliation observations after a valid lease. Require a stable distinction between `not-created` acquisition failures and `created` failures whose adapter-owned cleanup was attempted once. RED command: `npm test -- tests/release-candidate.test.ts -t "cleans every post-create outcome exactly once"`. Expected RED: current acquisition can throw after creation without cleanup evidence and several malformed boundaries bypass the single cleanup funnel; unexpected GREEN requires stopping. Commit tests as `test(04-05): require cleanup after candidate creation`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "cleans every post-create outcome exactly once"</automated>
  </verify>
  <done>The fault matrix fails specifically on missing, repeated, or misclassified post-create cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict acquisition and one cleanup funnel</name>
  <files>src/release-candidate.ts</files>
  <behavior>
    - Candidate acquisition returns a strict versioned not-created, acquired, or created-failure outcome with cleanup ownership unambiguous.
    - Every acquired opaque lease is cleaned once in `finally`, regardless of downstream parser or adapter behavior.
    - If creation occurs before acquisition can return a valid lease, the identity-bound adapter performs and reports exactly one cleanup attempt rather than emitting a precreation blocker.
  </behavior>
  <action>Redesign the factory acquisition boundary as a guarded versioned contract that distinguishes no-created-state failure from an acquired lease and from a post-create failure with adapter-owned cleanup evidence. Store the opaque identity before exposing a lease. Require the provider operation to clean exactly once internally if it creates state but cannot return a valid lease; malformed/exceptional outcomes become cleanup-unknown failures with attempted evidence, never `CANDIDATE_CREATION_FAILED`. Route every valid lease branch through one `try/finally` cleanup call and remove early post-acquisition returns. Do not retry cleanup or fall back to raw `rm`. Make the focused test GREEN, run lifecycle regressions, typecheck, and build. Commit production code as `feat(04-05): guarantee post-create cleanup`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "cleans every post-create outcome exactly once" &amp;&amp; npm test -- tests/release-candidate.test.ts tests/install-simulation.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>Every stateful creation path produces exactly one cleanup attempt/evidence record and no test candidate remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Adapter creation to orchestrator lease ownership | State may exist before a trustworthy lease result is available. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-11 | Repudiation | acquisition/cleanup lifecycle | mitigate | Version creation state and record one cleanup attempt after every creation. |
| T-04-12 | Denial of Service | orphan temporary candidate | mitigate | Transfer cleanup ownership atomically and funnel all acquired outcomes through `finally`. |
</threat_model>

<verification>
Run the post-create matrix, candidate/install-simulation regressions, typecheck, build, `git diff --check`, and inspect temp fixture absence.
</verification>

<success_criteria>
- RED precedes GREEN with atomic commits.
- Every post-create outcome has exactly one cleanup attempt and evidence record.
- No raw-path cleanup fallback, retry, persistent candidate, or precreation misclassification exists.
</success_criteria>

<output>
Create `.planning/phases/04-integrity-manifest-and-verified-cleanup/04-05-SUMMARY.md` when done.
</output>
