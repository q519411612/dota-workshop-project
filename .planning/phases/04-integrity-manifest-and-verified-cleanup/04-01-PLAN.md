---
phase: 04-integrity-manifest-and-verified-cleanup
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/release-candidate.test.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCIN-01
must_haves:
  truths:
    - "Users receive a blocker unless every accepted file has identical source-before, candidate, and source-after byte counts and SHA-256 values."
    - "Integrity observations are streamed through accepted source identities and the opaque candidate lease without exposing or rereading raw paths."
    - "After callback success or throw, fresh candidate and source-after observations must complete an exact triple comparison immediately before cleanup."
    - "Malformed, missing, duplicated, unsafe-count, or invalid-digest observations fail explicitly without retry, recopy, repair, or whole-file buffering."
  artifacts:
    - path: "src/release-candidate.ts"
      provides: "Strict versioned identity-bound streaming observations, final re-observation ordering, and triple-equality validation"
      exports: ["createIdentityBoundCandidateLifecycle", "withAssembledReleaseCandidate"]
    - path: "tests/release-candidate.test.ts"
      provides: "Streaming, hostile-result, and source-before/candidate/source-after mismatch fixtures"
  key_links:
    - from: "accepted source inventory"
      to: "source-before and source-after integrity observations"
      via: "identity-bound adapter operations over opened accepted identities"
      pattern: "observeAcceptedSource"
    - from: "opaque candidate lease"
      to: "candidate integrity observations"
      via: "lease-bound adapter operation with no raw candidate path read"
      pattern: "observeCandidate"
    - from: "inspection callback completion or throw"
      to: "final integrity comparison before cleanup"
      via: "fresh lease-bound candidate observations, fresh source-after observations, and exact triple equality"
      pattern: "observeCandidate|cleanupCandidateLease"
---

<objective>
Prove copied bytes with strict identity-bound streaming SHA-256 observations at the final pre-cleanup boundary.

Purpose: Close RCIN-01 by requiring exact source-before/source-after/candidate equality for every accepted regular file.
Output: Versioned integrity observation contracts, guarded normalization, streamed hashing, and deterministic mismatch blockers.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-CONTEXT.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-RESEARCH.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-PATTERNS.md
@.planning/phases/03-safe-candidate-assembly/03-VERIFICATION.md
@.planning/phases/03-safe-candidate-assembly/03-REVIEW.md
@src/release-candidate.ts
@tests/release-candidate.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify identity-bound streamed triple observations</name>
  <files>tests/release-candidate.test.ts</files>
  <behavior>
    - Every regular file has a source-before baseline; after callback success or throw, the lifecycle obtains fresh lease-bound candidate observations and fresh source-after observations immediately before cleanup and requires all source-before/candidate/source-after root/path/bytes/SHA-256 tuples to match.
    - Pre-callback candidate observations cannot satisfy the final integrity comparison.
    - Callback success without mutation can pass the final triple comparison; callback mutation and mutation-before-throw produce a stable integrity blocker.
    - Chunked large-file fixtures prove the production adapter consumes bounded chunks and never requests or returns a whole-file byte array.
    - Missing, duplicate, unexpected, identity/kind/containment-false, uppercase/non-64-hex digest, negative/fractional/unsafe byte count, getter, Proxy, iterator, thenable, and thrown results fail with stable sanitized evidence.
    - A mismatch or observation failure performs no hash retry, recopy, source write, compile, or repair.
  </behavior>
  <action>Extend the controlled macOS capability fixture with explicit source-integrity and lease-bound candidate-integrity observation seams, callback mutation hooks, and call counters ordered around inspection and cleanup. Add callback success, callback throw, callback mutation, and mutation-before-throw cases; require fresh candidate and source-after observations after callback settles and before the single cleanup call. Assert pre-callback candidate observations cannot satisfy the final comparison and a final mismatch returns a stable integrity blocker without retry or callback value. Add a large multi-chunk binary case, per-leg mismatch cases, and hostile result cases. Assert candidate observation requires the opaque lease and no absolute path, raw bytes, exception text, or secret-shaped identity is serialized. Keep the focused assertions limited to observation parsing, final re-observation ordering, triple comparison, callback mutation/throw detection, and cleanup ordering. RED command: `npm test -- tests/release-candidate.test.ts -t "reobserves triple integrity after inspection before cleanup"`. Expected RED: the lifecycle lacks final post-inspection candidate/source observation or can accept stale pre-callback equality after callback mutation or throw; unexpected GREEN requires stopping. Commit only tests as `test(04-01): require final integrity observation`, excluding `.planning/graphs/`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "reobserves triple integrity after inspection before cleanup"</automated>
  </verify>
  <done>The focused test fails for absent final post-inspection observations, absent triple comparison, or reuse of stale pre-callback equality, not for syntax/import errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict streaming integrity validation</name>
  <files>src/release-candidate.ts</files>
  <behavior>
    - The adapter streams opened identity bytes into Node SHA-256 and counts exact chunk bytes without retaining complete file content.
    - Guarded parsers accept only the fixed observation version, exact expected root/path, non-negative safe integer bytes, lowercase 64-hex digest, and literal true identity/kind/containment facts.
    - After callback success or throw and before cleanup, the lifecycle freshly observes candidate and source-after identities and compares every source-before/candidate/source-after triple exactly.
    - Pre-callback candidate observations cannot replace the final re-observation, and callback mutation cannot leave a successful integrity outcome or value.
  </behavior>
  <action>Add versioned source and candidate integrity observation methods to `IdentityBoundCandidateLifecycle` and its factory, keeping source access bound to validated inventory entries and candidate access bound to the lease WeakMap identity. Use Node `createHash("sha256")` incrementally inside adapter-owned observation operations; remove whole-file byte retention from integrity/stability observations while preserving directory topology checks. Normalize every foreign observation property inside guarded boundaries. Capture source-before once; after the callback settles—whether it returns or throws—and immediately before cleanup, obtain fresh lease-bound candidate plus source-after observations and require exact root/path/bytes/SHA-256 equality against the baseline. Callback throw remains an operation failure even when final bytes match, while callback mutation or mutation-before-throw returns a stable integrity blocker. Never retry, recopy, repair, or reuse a pre-callback observation as final. Expose the final validated observation seam for dependent plans without implementing their evidence domains. Do not add public MCP, schema/server/remote behavior, or host filesystem fallback. Make the focused test GREEN, then run candidate/readiness regressions, typecheck, and build. Commit only production code as `feat(04-01): verify final candidate integrity`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "reobserves triple integrity after inspection before cleanup" &amp;&amp; npm test -- tests/release-candidate.test.ts tests/release-readiness.test.ts tests/preflight.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>Fresh post-inspection candidate and source-after observations complete exact triple equality before cleanup; callback mutation cannot preserve a successful integrity outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutable source/candidate filesystem to domain validation | Adapter observations may be stale, malformed, duplicated, or attacker-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-01 | Tampering | source/candidate byte observations | mitigate | Bind reads to opened identities and require exact triple equality. |
| T-04-02 | Information Disclosure | observation errors | mitigate | Return stable codes and sanitized relative identities only. |
| T-04-03 | Denial of Service | large-file hashing | mitigate | Stream bounded chunks and reject impossible byte counts. |
</threat_model>

<verification>
Run the focused final-integrity test covering callback success, throw, mutation, and mutation-before-throw; run candidate/readiness/preflight regressions, typecheck, build, `git diff --check`, and confirm graph files are unstaged and unchanged by the plan.
</verification>

<success_criteria>
- RED precedes GREEN with isolated test and production commits.
- Every regular file requires final post-inspection source-before/candidate/source-after byte and SHA-256 equality before cleanup.
- Callback mutation or mutation-before-throw yields a stable integrity blocker; callback throw cannot return a value; cleanup remains exactly once.
- Higher-level evidence composition remains owned by dependent plans.
- No whole-file buffering, raw-path hashing, retry, fallback, source mutation, or Phase 5 integration is introduced.
</success_criteria>

<output>
Create `.planning/phases/04-integrity-manifest-and-verified-cleanup/04-01-SUMMARY.md` when done.
</output>
