---
phase: 04-integrity-manifest-and-verified-cleanup
plan: 02
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/release-candidate.test.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCIN-02
  - RCIN-03
must_haves:
  truths:
    - "Only the final post-inspection triple observations validated by 04-01 can become manifest input."
    - "Users receive one schema-versioned manifest entry per validated candidate file in deterministic ordinal root/path order."
    - "Every entry contains only root provenance, normalized candidate-relative path, exact bytes, and lowercase SHA-256 alongside its schema version."
    - "Identical logical candidates produce one fixed combined digest regardless of enumeration order, separators, absolute/temp paths, timestamps, permissions, locale, or host identity."
  artifacts:
    - path: "src/release-candidate.ts"
      provides: "Versioned ordinal manifest projection and collision-free canonical combined digest"
    - path: "tests/release-candidate.test.ts"
      provides: "Known-vector, permutation, delimiter, Unicode, and host-independence manifest tests"
  key_links:
    - from: "validated triple integrity observations"
      to: "manifest entries"
      via: "fixed-field projection from the final post-inspection triples, inventory provenance, and normalized candidate identity"
      pattern: "schemaVersion|sha256"
    - from: "ordinal manifest entries"
      to: "combined SHA-256"
      via: "UTF-8 JSON serialization of fixed nested arrays"
      pattern: "JSON.stringify"
---

<objective>
Project validated file observations into a deterministic versioned manifest and canonical combined digest.

Purpose: Close RCIN-02 and RCIN-03 without allowing host metadata or ambiguous delimiters into artifact identity.
Output: Fixed manifest types, ordinal ordering, collision-free canonical serialization, and independent known-vector tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-CONTEXT.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-RESEARCH.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-PATTERNS.md
@.planning/phases/04-integrity-manifest-and-verified-cleanup/04-01-SUMMARY.md
@src/release-candidate.ts
@src/source-snapshot.ts
@tests/release-candidate.test.ts
@tests/source-snapshot.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify fixed manifest and canonical digest vectors</name>
  <files>tests/release-candidate.test.ts</files>
  <behavior>
    - Manifest input is the final post-inspection source-before/candidate/source-after triple set from 04-01, never a pre-callback candidate snapshot.
    - Entries use the fixed version and contain exactly version/root/path/bytes/lowercase SHA-256 in explicit code-unit root/path order.
    - Shuffled adapter enumeration and source separator forms normalize to byte-identical manifests.
    - Alternate absolute roots, temp names, timestamps, permissions, target/host fields, and locale settings do not change the combined digest.
    - Delimiter-like, control, quote, backslash, and Unicode-safe relative identities remain collision-free under the canonical encoding.
  </behavior>
  <action>Add a focused manifest fixture with hard-coded expected entry order, an independently written fixed canonical UTF-8 byte string, and its known SHA-256 vector. Permute enumeration and irrelevant host metadata, and compare exact manifest/digest equality. Add adversarial safe paths that would collide under delimiter joining. RED command: `npm test -- tests/release-candidate.test.ts -t "builds deterministic canonical release candidate manifests"`. Expected RED: no versioned manifest or combined digest exists; unexpected GREEN requires stopping. Commit only tests as `test(04-02): require canonical candidate manifest`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "builds deterministic canonical release candidate manifests"</automated>
  </verify>
  <done>The known-vector test fails because the lifecycle does not yet return the fixed manifest and digest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ordinal fixed-field manifest projection</name>
  <files>src/release-candidate.ts</files>
  <behavior>
    - Manifest entries derive root/path identity only from accepted inventory and validated candidate observations.
    - Ordering uses an explicit ordinal comparator over root then normalized path and never locale-sensitive comparison.
    - The combined digest hashes UTF-8 JSON containing only the manifest version and fixed nested entry tuples.
  </behavior>
  <action>Define immutable internal manifest entry and manifest result types with a fixed supported version. Accept only the final post-inspection validated triple set exposed by 04-01; never project manifest evidence from a pre-callback candidate snapshot. Sort validated file entries with explicit code-unit comparison over root then path. Construct a nested-array canonical value containing only version and `[root,path,bytes,sha256]` tuples, serialize with `JSON.stringify`, and hash its UTF-8 bytes using Node SHA-256. Exclude absolute/temp paths, generated time, permissions, locale, target/host identity, warnings, blockers, scan coverage, and cleanup. Do not use object-record serialization, delimiter concatenation, `localeCompare`, source-snapshot host fields, or a new package. Make the focused test GREEN and run source-snapshot compatibility, typecheck, and build. Commit production code as `feat(04-02): produce canonical candidate manifest`.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "builds deterministic canonical release candidate manifests" &amp;&amp; npm test -- tests/source-snapshot.test.ts tests/release-candidate.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>Validated candidate files yield the exact versioned ordinal manifest and host-independent known combined digest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated observations to canonical artifact identity | Non-canonical ordering or host fields could produce misleading digests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-04 | Repudiation | combined manifest digest | mitigate | Hash one fixed versioned nested-array serialization with ordinal ordering. |
| T-04-05 | Tampering | manifest entry provenance | mitigate | Derive identities from accepted inventory, not adapter absolute paths. |
</threat_model>

<verification>
Run known-vector and permutation tests, source-snapshot regressions, typecheck, build, and `git diff --check`.
</verification>

<success_criteria>
- RED precedes GREEN with atomic commits.
- Entries and combined digest are deterministic under every excluded host/path permutation.
- Canonical input contains no ambiguous join, object-key-order dependency, host metadata, or cleanup state.
</success_criteria>

<output>
Create `.planning/phases/04-integrity-manifest-and-verified-cleanup/04-02-SUMMARY.md` when done.
</output>
