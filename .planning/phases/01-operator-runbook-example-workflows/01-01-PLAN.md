---
wave: 1
depends_on: []
requirements_addressed: [RUN-01, RUN-02, RUN-03, RUN-04, RUN-05, RUN-06]
autonomous: true
---

# Phase 1 Plan: Operator Runbook and Examples

## Objective

Add checked operator documentation and reusable safe workflow examples without changing runtime MCP behavior.

## Tasks

<task id="01-01-T1" type="tdd">
<title>Add example/runbook tests first</title>
<read_first>
- `.planning/REQUIREMENTS.md`
- `.planning/phases/01-operator-runbook-example-workflows/01-SPEC.md`
- `src/schemas.ts`
- `src/tools.ts`
- `README.md`
</read_first>
<action>
Create `tests/examples.test.ts`. Tests must assert required docs/examples exist, parse each JSON example, validate each `input` against the schema matching `operation`, compare operations against `toolNames`, scan docs/examples for forbidden private or credential-like patterns, and verify README links.
</action>
<acceptance_criteria>
- `npx vitest run tests/examples.test.ts` fails before docs/examples are created.
- Failure is due to missing runbook/examples, not TypeScript syntax errors.
</acceptance_criteria>
</task>

<task id="01-01-T2" type="documentation">
<title>Add operator runbook and safe examples</title>
<read_first>
- `README.md`
- `skills/dota2-workshop-tools/SKILL.md`
- `src/schemas.ts`
- `src/tools.ts`
</read_first>
<action>
Create `docs/operator-runbook.md` and four example files under `examples/workflows/`: `fixture-create-addon.json`, `fixture-preflight.json`, `fixture-release-dry-run.json`, and `remote-playable-smoke.template.json`. Use only fixture targets or placeholder remote values. Document that runtime success requires marker/log evidence and that cleanup must be explicit and addon-scoped.
</action>
<acceptance_criteria>
- `npx vitest run tests/examples.test.ts` passes.
- Remote template uses placeholder host and username values only.
- No example contains real private host, username, password, token, key, or Steam credential material.
</acceptance_criteria>
</task>

<task id="01-01-T3" type="documentation">
<title>Link examples from README</title>
<read_first>
- `README.md`
- `docs/operator-runbook.md`
</read_first>
<action>
Add README links to `docs/operator-runbook.md` and `examples/workflows/`, and state that examples are safe templates rather than real upload automation.
</action>
<acceptance_criteria>
- README contains `docs/operator-runbook.md`.
- README contains `examples/workflows/`.
- README contains `safe templates` and `not upload automation`.
</acceptance_criteria>
</task>

<task id="01-01-T4" type="verification">
<title>Verify and close v1.5</title>
<read_first>
- `.planning/phases/01-operator-runbook-example-workflows/01-SPEC.md`
- `.planning/phases/01-operator-runbook-example-workflows/01-01-PLAN.md`
- `tests/examples.test.ts`
</read_first>
<action>
Run targeted and full verification, strict secret scan, write `01-VERIFICATION.md`, `01-REVIEW.md`, and `01-01-SUMMARY.md`, update planning docs to complete, then commit and push only v1.5 changes while leaving graphify freshness files uncommitted.
</action>
<acceptance_criteria>
- `npx vitest run tests/examples.test.ts` exits 0.
- `git diff --check`, `npm run typecheck`, `npm test`, `npm run build`, and `npm run verify:plugin` exit 0.
- Strict high-signal secret scan reports no matches.
- Independent review records boundary compliance.
- Graphify freshness files remain uncommitted.
</acceptance_criteria>
</task>

## Artifacts This Phase Produces

- `docs/operator-runbook.md`
- `examples/workflows/fixture-create-addon.json`
- `examples/workflows/fixture-preflight.json`
- `examples/workflows/fixture-release-dry-run.json`
- `examples/workflows/remote-playable-smoke.template.json`
- `tests/examples.test.ts`
- README links to runbook and examples
- `.planning/phases/01-operator-runbook-example-workflows/01-VERIFICATION.md`
- `.planning/phases/01-operator-runbook-example-workflows/01-REVIEW.md`
- `.planning/phases/01-operator-runbook-example-workflows/01-01-SUMMARY.md`

## Must Haves

truths:
- Operator runbook exists and describes the safe workflow order.
- Examples exist and validate against existing schemas.
- Examples and runbook pass forbidden private/credential material scan.
- README links to the runbook and examples.

prohibitions:
- statement: Must not store credentials, tokens, private keys, private hosts, passwords, or private target fragments.
  status: resolved
  verification: test
- statement: Must not perform real Workshop upload, Steam login, Steam Guard, encryption, or publish-state mutation.
  status: resolved
  verification: judgment
- statement: Must not add new MCP operations or runtime behavior in this slice.
  status: resolved
  verification: judgment
- statement: Must not treat process launch as validation success.
  status: resolved
  verification: judgment
- statement: Must not silently accept missing docs, malformed examples, unknown operations, schema failures, or forbidden patterns.
  status: resolved
  verification: test

## Verification Commands

- `npx vitest run tests/examples.test.ts`
- `git diff --check`
- `npm run typecheck`
- `npm test`
- `npm run build`
- `npm run verify:plugin`
- Strict high-signal secret scan
