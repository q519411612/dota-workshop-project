---
wave: 1
depends_on: []
requirements_addressed: [VAL-01, VAL-02, VAL-03, VAL-04, VAL-05]
autonomous: true
---

# Phase 1 Plan: Windows Validation Closure

## Objective

Collect sanitized Windows validation evidence from a user-provided host and record whether the remaining local/same-machine smoke gap is closed, while preserving credential hygiene and existing no-upload boundaries.

## Tasks

<task id="01-01-T1" type="validation">
<title>Probe Windows host without storing credentials</title>
<read_first>
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `skills/dota2-workshop-tools/references/remote-control.md`
</read_first>
<action>
Use runtime-only SSH to probe the user-provided Windows host. Verify the shell responds and collect Dota/Workshop path categories without writing host, username, password, or private target details to project files.
</action>
<acceptance_criteria>
- SSH command succeeds or explicit connection failure evidence is captured.
- Verification notes refer only to a user-provided Windows host.
- No private connection details are written to repository files.
</acceptance_criteria>
</task>

<task id="01-01-T2" type="validation">
<title>Run smallest existing smoke path</title>
<read_first>
- `README.md`
- `src/smoke.ts`
- `src/remote.ts`
- `src/cleanup.ts`
</read_first>
<action>
Run the smallest practical existing validation path on the Windows host. Prefer playable smoke with runtime marker validation. If launch is blocked, record the blocker and run non-launch checks such as addon generation, inspection, and dry-run release report.
</action>
<acceptance_criteria>
- Runtime success is claimed only if expected log or console markers are found.
- Any partial fallback is labeled partial and does not claim runtime validation closure.
- Command evidence is summarized without private target details.
</acceptance_criteria>
</task>

<task id="01-01-T3" type="validation">
<title>Perform safe addon-scoped cleanup if needed</title>
<read_first>
- `src/cleanup.ts`
- `tests/cleanup.test.ts`
- `skills/dota2-workshop-tools/references/remote-control.md`
</read_first>
<action>
If a smoke process is running or blocking validation, first perform dry-run addon-scoped matching and only stop processes whose command line matches the generated smoke addon.
</action>
<acceptance_criteria>
- Dry-run cleanup evidence exists before execute cleanup.
- No broad Dota or Steam cleanup is performed.
- If cleanup is not needed, verification states that explicitly.
</acceptance_criteria>
</task>

<task id="01-01-T4" type="documentation">
<title>Record sanitized closure artifacts</title>
<read_first>
- `.planning/phases/01-windows-validation-closure/01-SPEC.md`
- `.planning/PROJECT.md`
- `.planning/STATE.md`
- `.planning/ROADMAP.md`
</read_first>
<action>
Write `01-VERIFICATION.md`, `01-REVIEW.md`, and `01-01-SUMMARY.md`. Update project state to show whether the Windows validation closure is complete or partial.
</action>
<acceptance_criteria>
- Artifacts contain no private host, username, password, token, key, or Steam credential data.
- Artifacts state exact verified categories and residual risk.
- Project planning docs reflect the outcome.
</acceptance_criteria>
</task>

<task id="01-01-T5" type="verification">
<title>Run local verification and hygiene checks</title>
<read_first>
- `package.json`
- `.planning/phases/01-windows-validation-closure/01-VERIFICATION.md`
- `.planning/phases/01-windows-validation-closure/01-REVIEW.md`
</read_first>
<action>
Run local verification commands, strict secret scan, independent review, then commit and push only v1.3 changes while leaving graphify freshness files uncommitted.
</action>
<acceptance_criteria>
- `git diff --check` exits 0.
- `npm run typecheck` exits 0.
- `npm test` exits 0.
- `npm run build` exits 0.
- Strict secret scan reports no stored credentials or private target data.
- Graphify freshness files remain uncommitted.
</acceptance_criteria>
</task>

## Artifacts This Phase Produces

- `.planning/REQUIREMENTS.md` for v1.3 validation closure
- `.planning/phases/01-windows-validation-closure/01-SPEC.md`
- `.planning/phases/01-windows-validation-closure/01-01-PLAN.md`
- `.planning/phases/01-windows-validation-closure/01-VERIFICATION.md`
- `.planning/phases/01-windows-validation-closure/01-REVIEW.md`
- `.planning/phases/01-windows-validation-closure/01-01-SUMMARY.md`

## Must Haves

truths:
- Windows target details must remain runtime-only and sanitized from repository artifacts.
- Missing Dota or Workshop paths must be explicit failures.
- Runtime validation success requires expected marker evidence.
- Cleanup must be addon-scoped.

prohibitions:
- statement: Must not store Windows password, private host, private username, tokens, private keys, or Steam credentials.
  status: resolved
  verification: test
- statement: Must not perform Workshop upload, Steam login, encryption, or publish-state mutation.
  status: resolved
  verification: judgment
- statement: Must not treat launch success as runtime validation success.
  status: resolved
  verification: judgment
- statement: Must not clean up broad unrelated Dota or Steam processes.
  status: resolved
  verification: judgment

## Verification Commands

- `git diff --check`
- `npm run typecheck`
- `npm test`
- `npm run build`
- Strict secret scan
