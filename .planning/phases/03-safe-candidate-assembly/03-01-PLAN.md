---
phase: 03-safe-candidate-assembly
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/preflight.test.ts
  - tests/release-readiness.test.ts
  - src/release-readiness.ts
  - src/preflight.ts
autonomous: true
requirements:
  - RCFS-04
must_haves:
  truths:
    - "Users receive one shared required-structure, metadata, placeholder, and redacted sensitive-material policy from both release inspection paths."
    - "Existing dry_run_release_report results remain exactly unchanged for representative success and blocker fixtures."
    - "Unsafe or unscannable required text is represented by safe relative path and category without exposing contents, credentials, or private roots."
  artifacts:
    - path: "src/release-readiness.ts"
      provides: "Pure structured release-readiness findings and compatibility-safe ordering"
      exports: ["evaluateReleaseReadiness"]
    - path: "tests/release-readiness.test.ts"
      provides: "Policy behavior and redaction tests"
    - path: "tests/preflight.test.ts"
      provides: "Exact dry-run characterization coverage"
  key_links:
    - from: "src/preflight.ts"
      to: "src/release-readiness.ts"
      via: "dryRunReleaseReport delegates decisions and renders the established strings"
      pattern: "evaluateReleaseReadiness"
---

<objective>
Extract the established release-readiness rules into one structured policy without changing the public dry-run operation.

Purpose: Close RCFS-04 through a single policy authority while preserving the locked inspection-only dry_run_release_report contract.
Output: Characterization tests, pure readiness findings, and a compatibility renderer in the existing preflight service.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-safe-candidate-assembly/03-CONTEXT.md
@.planning/phases/03-safe-candidate-assembly/03-RESEARCH.md
@.planning/phases/03-safe-candidate-assembly/03-PATTERNS.md
@src/preflight.ts
@src/result.ts
@tests/preflight.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize readiness and dry-run compatibility</name>
  <files>tests/preflight.test.ts, tests/release-readiness.test.ts</files>
  <behavior>
    - Complete metadata and required structure produce ordered positive findings.
    - Missing structure, missing metadata, and placeholder values produce the current ordered blockers.
    - Sensitive, oversized required-text, and unreadable required-text observations produce category-only structured findings with safe relative paths.
    - Representative dryRunReleaseReport success, metadata-blocked, placeholder-blocked, sensitive, invalid-addon, and missing-root results retain complete equality, including warnings, evidence order, blocker count, paths, commands, and logs.
  </behavior>
  <action>Add exact characterization fixtures before extraction. Capture complete representative ToolResult values rather than weakening existing assertions. Add policy tests that import evaluateReleaseReadiness and assert structured findings never serialize injected secret values or private absolute roots. Keep non-text inclusion separate from text scan policy and do not introduce manifest or scan-coverage accounting. RED command: `npm test -- tests/release-readiness.test.ts tests/preflight.test.ts`. Expected RED: the new test suite fails because `src/release-readiness.ts` and `evaluateReleaseReadiness` do not exist; if it passes, stop and correct the test. After observing RED, stage only the two test files, confirm `.planning/graphs/` is absent from `git diff --cached --name-only`, and commit `test(03-01): characterize release policy`.</action>
  <verify>
    <automated>npm test -- tests/release-readiness.test.ts tests/preflight.test.ts</automated>
  </verify>
  <done>The focused command has a recorded expected RED failure and the atomic test commit contains only characterization tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract structured policy with compatibility rendering</name>
  <files>src/release-readiness.ts, src/preflight.ts</files>
  <behavior>
    - evaluateReleaseReadiness returns stable codes, fields, categories, safe relative paths, and deterministic order for required structure, metadata, placeholders, and sensitive/unscannable text.
    - dryRunReleaseReport delegates policy decisions yet returns the exact pre-extraction public envelope and strings.
    - No finding contains matched content, raw filesystem exceptions, link targets, credentials, or private absolute roots.
  </behavior>
  <action>Move the authoritative constants and classification decisions from preflight.ts into a pure release-readiness module, keeping filesystem observations and the compatibility renderer at explicit seams. Preserve required-path, metadata-key, scan-root, secret-pattern, evidence, blocker, and warning order. Candidate callers must be able to treat required-text unreadable/oversized findings as blockers later, while the existing dry-run renderer preserves its established behavior exactly. Do not alter schemas, ToolResult, tool registration, or dry_run_release_report operation semantics. Run the focused command until GREEN, then `npm run typecheck` and `npm run build`. Stage only the two source files, verify graphs remain unstaged, and commit `feat(03-01): share release policy`.</action>
  <verify>
    <automated>npm test -- tests/release-readiness.test.ts tests/preflight.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>Shared structured policy tests pass, exact dry-run characterizations pass unchanged, and the atomic source commit contains no public MCP changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Addon files to readiness findings | Untrusted addon bytes and metadata become serialized evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Information Disclosure | release-readiness findings | mitigate | Emit stable category and relative identity only; serialization tests reject values and private roots. |
| T-03-02 | Tampering | dry-run compatibility renderer | mitigate | Exact complete-result characterization prevents policy extraction drift. |
</threat_model>

<verification>
`npm test -- tests/release-readiness.test.ts tests/preflight.test.ts`, `npm run typecheck`, `npm run build`, and `git diff --check` pass; staged-file checks exclude `.planning/graphs/`.
</verification>

<success_criteria>
- RED precedes GREEN and both atomic commits are present.
- RCFS-04 has one shared policy authority with redacted structured findings.
- dry_run_release_report behavior and public result shape are unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-safe-candidate-assembly/03-01-SUMMARY.md` when done.
</output>
