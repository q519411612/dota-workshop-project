---
phase: 03-safe-candidate-assembly
plan: 04
type: tdd
wave: 4
depends_on: ["03-03"]
files_modified:
  - tests/release-candidate.test.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCFS-01
must_haves:
  truths:
    - "Users can inspect a candidate created exactly once beneath a validated target-local temporary parent and canonically outside every prohibited root."
    - "The candidate exists only during the callback and is removed after callback success or failure without being returned as a persistent artifact."
    - "A post-creation canonical escape or uncertain removal is an explicit failure, never a fallback."
  artifacts:
    - path: "src/release-candidate.ts"
      provides: "Validated temporary-root creation and callback-scoped lifetime"
      exports: ["withAssembledReleaseCandidate"]
    - path: "tests/release-candidate.test.ts"
      provides: "Canonical isolation, callback lifetime, and removal-failure tests"
  key_links:
    - from: "src/release-candidate.ts"
      to: "validated Dota, source, repository, and temp-parent canonical roots"
      via: "precreation and post-mkdtemp relative containment checks"
      pattern: "createCandidateRoot"
---

<objective>
Create a target-local candidate only after canonical isolation is proven and keep its lifetime callback-scoped.

Purpose: Close RCFS-01 without introducing persistent artifacts or Phase 4's formal cleanup evidence contract.
Output: Canonical isolation enforcement, single creation, inspection lifetime, and explicit removal failure.
</objective>

<execution_context>
@/Users/chenminghui/.codex/gsd-core/workflows/execute-plan.md
@/Users/chenminghui/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-safe-candidate-assembly/03-CONTEXT.md
@.planning/phases/03-safe-candidate-assembly/03-RESEARCH.md
@.planning/phases/03-safe-candidate-assembly/03-PATTERNS.md
@.planning/phases/03-safe-candidate-assembly/03-03-SUMMARY.md
@src/release-candidate.ts
@tests/release-candidate.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify canonical isolation and temporary lifetime</name>
  <files>tests/release-candidate.test.ts</files>
  <behavior>
    - Temp parents inside or canonically aliased to the Dota root, either addon source, or repository block with zero creation.
    - A safe external temp parent creates exactly one candidate, which exists during inspection and is absent after success and callback failure.
    - An injected post-create canonical escape prevents layout writes and callback invocation; an injected removal failure is explicit and does not claim absence.
  </behavior>
  <action>Add real and adapter-injected canonical alias cases, creation counts, callback access checks, post-return access rejection, and recorded adapter write paths. Keep formal cleanup object/version/precedence assertions out of this plan. RED command: `npm test -- tests/release-candidate.test.ts -t "keeps the candidate canonically isolated and callback scoped"`. Expected RED: the valid callback is not reached or the temporary root lifetime/isolation/removal assertions fail because creation ownership is missing; unexpected GREEN requires stopping. Commit the test-only change as `test(03-04): require isolated lifetime` with graph files unstaged.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "keeps the candidate canonically isolated and callback scoped"</automated>
  </verify>
  <done>The missing canonical lifetime behavior produces the expected focused RED failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement single-owner candidate creation and removal</name>
  <files>src/release-candidate.ts</files>
  <behavior>
    - mkdtemp runs only beneath the already validated target-local parent and exactly once for an accepted request.
    - The created root is canonicalized and checked again before any descendant write.
    - A try/finally owner removes the candidate after all callback outcomes and maps removal uncertainty to an explicit internal failure.
  </behavior>
  <action>Use adapter mkdtemp under the validated parent, canonicalize the returned child, and prove it is strictly outside the Dota root, both source roots, and repository before any layout write. Invoke inspection only inside the owned lifetime and never return a usable candidate path. Remove in finally after success or callback failure; normalize removal errors without falsely claiming cleanup. Do not add versioned cleanup details or precedence rules owned by Phase 4. Make the focused test GREEN, then typecheck and build. Commit only the source file as `feat(03-04): own isolated lifetime` after verifying staged paths.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "keeps the candidate canonically isolated and callback scoped" &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>The callback is the sole inspection window, the candidate is canonically isolated, and every removal uncertainty fails explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Temporary-parent service to candidate root | A created path can alias protected roots or persist beyond ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-07 | Tampering | canonical candidate isolation | mitigate | Check canonical parent before creation and canonical child before descendant writes. |
| T-03-08 | Information Disclosure | candidate lifetime | mitigate | Callback ownership plus finally removal; explicit failure on uncertain removal. |
</threat_model>

<verification>
Focused isolation/lifetime tests, typecheck, build, `git diff --check`, and staged-file inspection pass; no persistent candidate or graph changes are committed.
</verification>

<success_criteria>
- RED precedes GREEN and atomic commits remain independently revertible.
- RCFS-01 canonical exclusions and target-local creation are covered on macOS fixtures.
- No persistent archive, retained candidate, formal cleanup payload, or fallback exists.
</success_criteria>

<output>
Create `.planning/phases/03-safe-candidate-assembly/03-04-SUMMARY.md` when done.
</output>
