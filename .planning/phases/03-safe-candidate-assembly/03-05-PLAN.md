---
phase: 03-safe-candidate-assembly
plan: 05
type: tdd
wave: 5
depends_on: ["03-04"]
files_modified:
  - tests/release-candidate.test.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCFS-02
must_haves:
  truths:
    - "Users can inspect exact game/dota_addons/<addon> and content/dota_addons/<addon> trees inside the temporary candidate."
    - "Every accepted regular file, hidden file, extensionless file, generated-looking file, binary file, and empty directory is included regardless of enumeration order."
    - "Readiness blockers are evaluated through the shared policy without filtering or repairing either source tree."
  artifacts:
    - path: "src/release-candidate.ts"
      provides: "Explicit deterministic two-root directory creation and regular-file copying"
      exports: ["withAssembledReleaseCandidate"]
    - path: "tests/release-candidate.test.ts"
      provides: "Complete layout, binary-byte, hidden-file, and empty-directory fixture coverage"
  key_links:
    - from: "src/release-candidate.ts"
      to: "src/release-readiness.ts"
      via: "precreation source observations use the shared readiness policy"
      pattern: "evaluateReleaseReadiness"
    - from: "source inventory identities"
      to: "candidate fixed-prefix paths"
      via: "explicit mkdir and copyFile operations"
      pattern: "game/dota_addons|content/dota_addons"
---

<objective>
Assemble the complete accepted game and content source trees beneath the fixed candidate layout.

Purpose: Close RCFS-02 with explicit deterministic operations that preserve all regular files and empty directories without heuristics.
Output: Full two-root assembly and callback-observable fixture proof.
</objective>

<execution_context>
@/Users/chenminghui/.codex/gsd-core/workflows/execute-plan.md
@/Users/chenminghui/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-safe-candidate-assembly/03-CONTEXT.md
@.planning/phases/03-safe-candidate-assembly/03-RESEARCH.md
@.planning/phases/03-safe-candidate-assembly/03-PATTERNS.md
@.planning/phases/03-safe-candidate-assembly/03-01-SUMMARY.md
@.planning/phases/03-safe-candidate-assembly/03-04-SUMMARY.md
@src/release-readiness.ts
@src/release-candidate.ts
@tests/release-candidate.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify complete fixed-layout assembly</name>
  <files>tests/release-candidate.test.ts</files>
  <behavior>
    - The inspection callback sees both exact fixed prefixes and byte-identical regular files beneath them.
    - Binary, extensionless, dotfile, ignore-named, generated-looking, old-timestamp, and nested files are included.
    - Nested empty directories in both roots exist, and shuffled adapter enumeration yields the same relative tree.
    - Shared readiness structure, metadata, placeholder, and redacted sensitive blockers prevent inspection without source modification.
  </behavior>
  <action>Build one complete macOS addon fixture plus focused variants for hidden, binary, extensionless, generated-looking, timestamp, empty-directory, and readiness blocker cases. Inspect bytes and directory presence only inside the callback; after return assert the candidate is absent. Do not assert manifests, hashes, scan-coverage counts, or formal cleanup payloads. RED command: `npm test -- tests/release-candidate.test.ts -t "assembles the complete fixed two-root layout"`. Expected RED: the callback sees missing fixed directories/files or is not invoked because explicit layout creation/copy is not implemented; unexpected GREEN requires stopping. Commit only the tests as `test(03-05): require complete two-root layout` with graphs excluded.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "assembles the complete fixed two-root layout"</automated>
  </verify>
  <done>The complete-layout fixtures produce the expected RED failure before production copy behavior exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit directory and file assembly</name>
  <files>src/release-candidate.ts</files>
  <behavior>
    - Every inventoried directory is created and every inventoried regular file is copied under its validated fixed-prefix destination.
    - Inclusion is independent of ignore files, names, extensions, timestamps, hidden status, or generated-file conventions.
    - Entry classification and canonical containment are revalidated immediately before each mkdir or copyFile.
  </behavior>
  <action>Convert the accepted sorted inventory into explicit mkdir and copyFile operations beneath the canonical candidate root. Create the fixed root directories and every source directory, including empty leaves. Before each use, reclassify the source entry, compare it with its inventory observation, recheck canonical containment, and validate the destination identity remains inside the candidate. Feed source observations to the shared readiness policy before creation; return its structured safe blockers without changing dry-run rendering. Never use recursive cp, filtering, compilation, generation, repair, rename, archive, or source writes. Make the focused test GREEN, run the readiness/preflight regression tests, typecheck, and build. Commit only release-candidate.ts as `feat(03-05): assemble complete addon trees` after staged-path review.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "assembles the complete fixed two-root layout" &amp;&amp; npm test -- tests/release-readiness.test.ts tests/preflight.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>The callback observes every accepted file and empty directory in the exact two-root layout with unchanged shared readiness behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Accepted source inventory to candidate writes | A stale or manipulated inventory could redirect writes or omit required content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-09 | Tampering | explicit assembly loop | mitigate | Reclassify and recheck containment immediately before each candidate write. |
| T-03-10 | Information Disclosure | inclusion/readiness split | mitigate | Include all regular bytes but expose only redacted readiness categories and safe identities. |
</threat_model>

<verification>
Focused layout and readiness compatibility suites, typecheck, build, `git diff --check`, and staged-file inspection pass with no graph or source-fixture mutation.
</verification>

<success_criteria>
- RED precedes GREEN with atomic test and implementation commits.
- RCFS-02 layout and inclusion categories are observable inside the callback.
- No ignore, extension, timestamp, hidden-file, or generated-file heuristic affects inclusion.
</success_criteria>

<output>
Create `.planning/phases/03-safe-candidate-assembly/03-05-SUMMARY.md` when done.
</output>
