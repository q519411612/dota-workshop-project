---
phase: 03-safe-candidate-assembly
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/release-candidate.test.ts
  - src/release-candidate.ts
autonomous: true
requirements:
  - RCOP-02
must_haves:
  truths:
    - "Users receive a stable blocker for every invalid addon, target root, game addon root, or content addon root before a candidate directory exists."
    - "No invalid request creates a partial candidate or selects another target."
  artifacts:
    - path: "src/release-candidate.ts"
      provides: "Validated release-candidate input state and internal result contract"
      exports: ["prepareReleaseCandidateInput"]
    - path: "tests/release-candidate.test.ts"
      provides: "Precreation gate and creation-spy coverage"
  key_links:
    - from: "src/release-candidate.ts"
      to: "src/addon.ts"
      via: "validateAddonName runs before filesystem work"
      pattern: "prepareReleaseCandidateInput"
---

<objective>
Make invalid or missing candidate inputs fail before any temporary candidate creation.

Purpose: Close RCOP-02 with a typed precreation state transition and explicit evidence.
Output: The internal callback-scoped service contract and exhaustive zero-creation input tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-safe-candidate-assembly/03-CONTEXT.md
@.planning/phases/03-safe-candidate-assembly/03-RESEARCH.md
@.planning/phases/03-safe-candidate-assembly/03-PATTERNS.md
@.planning/phases/03-safe-candidate-assembly/03-01-SUMMARY.md
@src/addon.ts
@src/install-simulation.ts
@src/release-readiness.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove every precreation blocker</name>
  <files>tests/release-candidate.test.ts</files>
  <behavior>
    - Invalid addon names, missing/nonexistent/non-directory Dota roots, and missing/non-directory game or content addon roots return stable structured blockers.
    - Unsafe or missing repository/temp-parent inputs also block before creation.
    - Every rejected case records zero createCandidateRoot or mkdtemp calls and never invokes inspection.
  </behavior>
  <action>Create macOS temporary fixtures and a narrow adapter spy for creation calls. Parameterize every invalid and missing input class and assert safe field/category evidence without raw exceptions or private paths. Use an operation input with addonName, dotaRoot, and tempParent; supply the trusted repository root through injectable internal dependencies defaulting to the current repository. Derive and independently validate the fixed game/content addon roots. RED command: `npm test -- tests/release-candidate.test.ts -t "blocks invalid inputs before candidate creation"`. Expected RED: import/export resolution or the zero-creation contract fails because release-candidate.ts and prepareReleaseCandidateInput are absent; unexpected GREEN requires stopping. Commit only the test file as `test(03-02): require precreation validation` after confirming graphs are unstaged.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "blocks invalid inputs before candidate creation"</automated>
  </verify>
  <done>The parameterized test fails for the expected missing behavior and the RED commit is isolated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the validated input state</name>
  <files>src/release-candidate.ts</files>
  <behavior>
    - prepareReleaseCandidateInput returns a discriminated validated state that is the only accepted input to inventory and creation seams.
    - Each invalid field maps to an explicit stable blocker and returns without candidate creation, fallback, or callback invocation.
    - Filesystem errors are normalized to safe codes and fields rather than forwarded verbatim.
  </behavior>
  <action>Define compact internal result, blocker, adapter, operation input, trusted dependency, and validated-state types following adjacent service conventions. Implement prepareReleaseCandidateInput to validate the addon name first, then resolve, classify, and canonicalize the Dota root, trusted repository root, temp parent, and derived addon roots. The function's complete responsibility is to return either explicit blockers or an immutable validated state; it does not own inventory, creation, copying, or inspection. Do not expose a public MCP result or add fallback behavior. Make the focused test GREEN, then typecheck and build. Commit only release-candidate.ts as `feat(03-02): validate assembly inputs` after checking the staged paths.</action>
  <verify>
    <automated>npm test -- tests/release-candidate.test.ts -t "blocks invalid inputs before candidate creation" &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>All input and source-root blockers occur before candidate creation with stable redacted evidence and no fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Request paths to filesystem adapter | Untrusted names and paths can select unintended filesystem locations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-03 | Tampering | input validation gate | mitigate | Validate type, existence, and canonical identity before any creation call. |
| T-03-04 | Information Disclosure | filesystem error normalization | mitigate | Return stable field/category evidence without raw exception messages or private roots. |
</threat_model>

<verification>
Focused RED/GREEN evidence, typecheck, build, `git diff --check`, and staged-file inspection prove the precreation gate and graph exclusion.
</verification>

<success_criteria>
- RED precedes GREEN with two atomic commits.
- Every RCOP-02 invalid or missing input class has a zero-creation assertion.
- No public schema, server, remote adapter, manifest, or formal cleanup result is introduced.
</success_criteria>

<output>
Create `.planning/phases/03-safe-candidate-assembly/03-02-SUMMARY.md` when done.
</output>
